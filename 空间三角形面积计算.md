# 平面直角坐标系计算
**平面上两个点**$A(x_1,y_1),B(x_2,y_2)$，**与坐标原点$O$，组成三角形$\bigtriangleup AOB$，$\overrightarrow{OA},\overrightarrow{OB}$与$x$轴正方向夹角为$\alpha ,\beta$，且$\beta -\alpha =\theta$**
**则$$S = |\frac{1}{2}(x_{1}y_{2} - x_{2}y_{1})| = |det\begin{pmatrix}
  x1& y1\\
  x2& y2
\end{pmatrix}|$$**
![[Pasted image 20260521102351.png]]
如图所示，记$\bigtriangleup AOB$面积为$S$,则
$$
S = \frac{1}{2}|\overrightarrow{OA}|\cdot |\overrightarrow{OB}|\sin \theta = \frac{1}{2}r_1 r_2 \sin(\beta \alpha)
$$
$$
=\frac{1}{2}r_1 r_2(\sin\beta \cos\alpha - \cos\beta \sin\alpha)
$$
$$
=\frac{1}{2}(r_2 \sin\beta \cdot r_1\cos\alpha - r_2 \cos\beta\cdot r_1\sin\alpha)
$$
$$
=\frac{1}{2}(x_1 y_2 - x_2 y_1)
$$
一般，平面上任意三点$A(x_1 ,y_1 ),B(x_2 ,y_2 ),P(x_3, y_3 )$构成$\bigtriangleup AOB$。
$$
\overrightarrow{PA} = (x_1 - x_3 ,y_1 - y_3),\overrightarrow{PB} = (x_2 - x_3 ,y_2 - y_3 )
$$
则
$$
S_{\bigtriangleup APB} = \frac{1}{2}|(x_1 - x_3)(y_2 - y_3)-(x_2 - x_3)(y_1 - y_3)|
$$
$$
=\frac{1}{2}|det\begin{pmatrix}
  x_1 - x_3 & y_1 - y_3 &0\\
  x_2 - x_3 & y_2 - y_3 &0\\
  0&0&1
\end{pmatrix}| = \frac{1}{2}|det\begin{pmatrix}
  x_1& y_1 & 1\\
  x_2& y_2 & 1\\
  x_3 & y_3 & 1
\end{pmatrix}|
$$
# 三维空间点
**三维空间点中，$A(x_1 ,y_1 ,z_1),B(x_2 ,y_2 ,z_2)$，与原点$O$构成三角形，则三角形的面积为：**
$$
S_{\bigtriangleup AOB} = \frac{1}{2} |\overrightarrow{OA}| |\overrightarrow{OB}| \sin\angle AOB
$$
$$
=\frac{1}{2}|\overrightarrow{OA}| |\overrightarrow{OB}| \sqrt{1- \cos^{2}\angle AOB}
$$
$$
=\frac{1}{2}\sqrt{|\overrightarrow{OA}|^{2}\cdot|\overrightarrow{OB}|^{2} - (\overrightarrow{OA} \cdot \overrightarrow{OB})^{2}}
$$

因为：
$$
|\overrightarrow{a} \times \overrightarrow{b}| = |\overrightarrow{a}||\overrightarrow{b}|\sin\theta
$$
其中$\theta = <a,b>$
所以：
$$
=\frac{1}{2}\sqrt{|\overrightarrow{OA}|^{2}\cdot|\overrightarrow{OB}|^{2}\sin^{2}\angle AOB}
$$
$$
=\frac{1}{2}|\overrightarrow{OA}||\overrightarrow{OB}|sin\angle AOB=\frac{1}{2}|\overrightarrow{OA} \times \overrightarrow{OB}|
$$
$$
=\frac{1}{2}\sqrt{(x_1 y_2 - x_2 y_1)^{2}+(x_1 z_2 - x_2 z_1)^{2}+(y_1 z_2 - y_2 z_1)^{2}}
$$
# 代码实现
``` c++
// 三维点，包含灰度值
struct Point3D {
    double x, y, z;
    double gray;
    Point3D(double x = 0, double y = 0, double z = 0, double gray = 0)
        : x(x), y(y), z(z), gray(gray) {}
};
// 向量运算
Point3D operator-(const Point3D& a, const Point3D& b) {
    return Point3D(a.x - b.x, a.y - b.y, a.z - b.z);
}
double dot(const Point3D& a, const Point3D& b) {
    return a.x * b.x + a.y * b.y + a.z * b.z;
}
Point3D cross(const Point3D& a, const Point3D& b) {
    return Point3D(
        a.y * b.z - a.z * b.y,
        a.z * b.x - a.x * b.z,
        a.x * b.y - a.y * b.x
    );
}
double norm(const Point3D& v) {
    return std::sqrt(v.x * v.x + v.y * v.y + v.z * v.z);
}
// 计算三角形面积（三维空间）
double triangleArea(const Point3D& a, const Point3D& b, const Point3D& c) {
    Point3D ab = b - a;
    Point3D ac = c - a;
    Point3D cp = cross(ab, ac);
    return 0.5 * norm(cp);
}
```