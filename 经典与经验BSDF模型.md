# 经典与经验BSDF模型

## Lambert漫反射模型

$$
f_{Lambert}(\omega_{i},\omega_{o}) = \frac{\rho_{d}}{\pi}
$$

其中$\rho_{d}$为漫反射率，取值范围通常为$[0,1]$。

**适用范围：**

哑光、无光泽、粗糙度均匀的材料，如粉笔、未打磨的混凝土、纸张、布料等。对于金属、塑料等有光泽表面无效(或许与菲涅尔，高斯组合可以有效表述)**。

## 高斯漫反射模型

高斯散射模型是另一种常用的基础散射模型，其散射角分布呈现高斯函数形式。该模型特别适用于描述具有中等粗糙度的抛光光学表面的散射特性
**数学公式：**
$$
BSDF_{Gaussian} = \frac{1}{\pi \sigma^{2}}exp(-\frac{sin^{2}\theta_{s}}{\sigma^{2}})
$$
或者适用方向余弦表示：
$$
BSDF_{Gaussian} = \frac{1}{\pi\sigma}exp(-\frac{(\alpha-\alpha_{0})^{2}+(\beta - \beta_{0})^{2}}{\sigma^{2}})
$$
其中：
- $\sigma$：无量纲的高斯宽度参数，决定了散射分布的角宽度，(0.01极窄的散射分布，接近光滑表面，0.1中等宽度的散射分布，0.5较宽的散射分布，接近漫射特性)
- $\theta_{s}$：散射角
- $\alpha、\beta$：散射方向的X、Y方向余弦（应该是水平与垂直方向的方向余弦）
- $\alpha_{0}、\beta_{0}$：镜面反射或折射方向的X、Y方向余弦（应该是水平与垂直方向的方向余弦）
## ABg散射模型
是一种专门用于光学散射分析的经验BSDF模型。适合描述**光滑表面的微观粗糙度引起的散射**
**数学公式：**
$$
BSDF=\frac{A}{B+|\vec{\beta_{o}}-\vec{\beta_{i}}|^{g}}
$$
或**常见的形式：**
$$
f = \frac{A}{B+|\sin{\theta_{o}-\sin{\theta_{i}}}|^{g}}
$$
其中：
- A：散射幅度参数，A值越大表面散射越强
- B：常数通常大于0，影响曲线从平坦到斜坡的过渡点
- g：幂指数
- $\theta_{i},\theta_{o}$：入射角和散射角
- $\vec{\beta_{o}},\vec{\beta_{i}}$：$\beta_{o}$为散射方向的正弦值或者投影向量(投影无法使用蒙特卡洛法进行计算)，$\beta_{i}$为镜面方向的正弦值或者投影向量(投影无法使用蒙特卡洛法进行计算)
**注意：**
方向余弦差$\beta_{o}-\beta_{i}$可以表示为：
$$
|\beta_{o}-\beta_{i}| = \sqrt{((\alpha-\alpha_{0})^{2}+(\beta - \beta_{0})^{2})} = \sin{\theta_{s}}
$$
因此上述公式可转化为：
$$
BSDF = \frac{A}{B+\sin^{g}{\theta_{s}}}
$$
**适用范围：**
**（1）表面粗糙度要求**：该模型适用于描述由于微小尺度随机各向同性表面粗糙度引起的散射 。需要表面粗糙度远小于光波长 ，这是"光滑表面"的定义条件。
**（2）表面特性假设**：假设表面是各向同性的 即表面的统计特性在各个方向上相同。适用于光滑光学表面 如抛光的透镜和反射镜。
**（3）特定表面限制**：某些特定表面（如衍射光栅）可能需要模型修正 ，因为这类表面具有规则的周期结构，不符合随机粗糙度的假设。
## Harvey-Shack散射模型
Harvey-Shack模型是一种用于描述表面散射特性的模型，对于光滑表面有很好的配适性。
**数学公式：**
$$
BSDF_{Harvey-Shack}(\beta-\beta_{o}) = b_{o}(1+(\frac{\beta-\beta_{o}}{L})^{2})^{\frac{s}{2}}
$$
其中：
- $b_{o}$：表示散射幅度，代表散射分布的高度或峰值
- $s$：表示倾斜因子，控制散射分布随角度变化的衰减速率
- $L$：表示Lambertian极限或翻转角。该参数决定了散射分布从近镜面区域的平坦响应过渡到角度依赖衰减的转折点。
- $\beta,\beta_{o}$：与ABg模型一致
另一种常见形式为：
$$
BSDF(\beta) = b_{o}[1+(\frac{\beta}{L})^{2}]^{\frac{s}{s}}
$$
其中$\beta = \sin{(散射角)}$
有其他文献指出有Harvey模型的BRDF表达式可能后续需要进行区分，公式如下：
$$
BRDF = b_{c}[1+(\frac{R}{L})^{2}]^{\frac{s}{2}}
$$
上述变体公式暂时的适用范围还不确定！！！

## Phong光照模型

$$
L_{Phong} = k_{a}L_{a}+k_{d}L_{i}(N\cdot L)+k_{s}L_{i}(V\cdot R)^{n}
$$

其中：

- $k_{a},k_{d},k_{s}:$环境光、漫反射、镜面反射系数。
- $L_{a},L_{i}:$环境光强度、光源强度。
- $N:$表面法线。
- $L:$光方向向量(从表面指向光源)
- $V:$视线方向向量(从表面指向观察者)。
- $R:$反射向量，$R = 2(N\cdot L)N-L$。
- $n:$镜面高光指数，控制高光锐利程度。

参数物理含义：

- $k_{d}:$模拟表面粗糙导致的漫反射。
- $k_{s}:$模拟表面光滑度导致的镜面反射。
- $n:$经验性参数，值越大高光越锐利，表面越光滑。

**适用范围：**

塑料、光泽表面等简单材料。不满足能量守恒，不满足互易性，无法表现菲涅尔效应、各向异性等复杂光学现象。

## Blinn-Phong改进模型

$$
L_{Blinn-Phong} = k_{a}L_{a}+k_{d}L_{i}(N\cdot L)+k_{s}L_{i}(N\cdot H)^{n}
$$

其中半程向量$H$定义为：$H = \frac{L+H}{||L+V||}$。

更物理准确的BRDF形式为：
$$
\rho_{Blinn-Phong}(L,V) = \frac{\rho_{d}}{\pi}+\rho_{s}\cdot \frac{n+2}{2\pi}(H\cdot N)^{n}
$$
**适用范围：**

适用于塑料、陶瓷、漆面等中等光泽表面。不满足能量守恒和互易性，高光形状仍与真实物理行为有差异，特别是在金属表面。

# 物理基础维表面BSDF模型

## Cook-Torrance微表面模型

$$
f_{Cook-Torrance} = \frac{D\cdot F\cdot G\cdot}{4(\omega_{o}\cdot n)(\omega_{i}\cdot n)}
$$

其中：

1. **法线分布函数**$D:$描述微面元法线的统计分布，决定高光的形状和强度
2. **菲涅尔项**$F:$描述反射率随入射角的变化，基于光在两种介质界面的物理行为
3. **几何遮蔽函数**$G:$描述微面元之间的相互遮挡效应

**微面分布类型可选择不同的**$D$**函数（如Beckmann、GGX），影响高光特性。**

**适用范围：**

适用于金属、塑料、玻璃等多种材质。但参数设置不直观，需要实验调整，在某些角度可能会违反能量守恒。未考虑多层材料、次表面散射等复杂现象。

## GGX/Trowbridge-Reitz法线分布

$$
D_{GGX}(h) = \frac{\alpha^{2}}{\pi ((n\cdot h)^{2}(\alpha^{2}-1)+1)^{2}}
$$

其中：

- $h:​$半程向量。
- $n:$表面法线。
- $\alpha:$粗糙度参数，与感知粗糙度线型相关。$\alpha = 0$对应完美镜面；$\alpha = 1$对应极粗糙表面。通常$\alpha = roughness^{2}$，$roughness​$为可调的感知粗糙度参数。

**适用范围：**铝、铜等金属的高光拖尾，塑料、漆面的自然反射，粗糙度变化的复杂表面。

## Oren-Nayar粗糙漫反射模型

$$
f_{Oren-Nayar} = \frac{\rho}{\pi}\cos \theta_{i}(A+B\cdot max[0,\cos(\Phi_{r}-\Phi_{i})]\cdot \sin\alpha \cdot \tan\beta)
$$

其中：

- $\rho:$反射率
- $\theta_{i},\theta_{r}:$入射角和反射角
- $\Phi_{i},\Phi_{r}:$入射角方位角和反射角方位角
- $\alpha = \max(\theta_{r},\theta_{i})$
- $\beta = \min(\theta_{r},\theta_{i})​$
- $A$和$B$是由粗糙度参数$\sigma$计算的系数：
  - $A = 1-0.5\frac{\sigma^{2}}{\sigma^{2}+0.33},B = 0.45\frac{\sigma^{2}}{\sigma^{2}+0.09}$
  - $\sigma:$表面粗糙度，表示微面法线方向角的标准差，取值$[0,\pi/2]$或者归一化到$[0,1]$，值越大表面粗糙
- $\rho:$材质反射率

**适用范围：**哑光、粗糙、非光泽材料。符合能量守恒，表现出逆向反射效应——掠射角时边缘变亮，各向同性。但，不能表现光泽。

# 各向异性与高级BSDF模型

## Ward各向异性反射模型

**Ward模型**由Gregory Ward于1992年提出，是一种**各向异性BSDF**的经验模型
**数学公式：**
$$
f_{Ward}(\omega_{i},\omega_{o}) = \frac{\rho_{d}}{\pi}+\rho_{s}\frac{1}{4\pi\alpha_{x}\alpha_{y}}\frac{exp(-tan^{2}\theta_{h}(\frac{cos^{2}\phi_{h}}{\alpha^{2}_{x}}+\frac{sin^{2}\phi_{h}}{\alpha^{2}_{y}}))}{\sqrt{cos\theta_{i} cos\theta_{o}}}
$$
![[Pasted image 20260416172527.png]]
![[Pasted image 20260416172543.png]]
## Ashikmin-Shirley各向异性BRDF
**Ashikmin-Shirley模型**是一种**更物理基础的各向异性BRDF**模型 。该模型满足能量守恒和互易性，并提供更灵活的高光控制。

## Disney Principled BSDF
**Disney Principled BSDF**由迪士尼动画工作室的Brent Burley于2012年提出，是一种**艺术家友好型**的物理基础BSDF模型 。该模型旨在通过**直观参数**实现广泛材料效果，同时保持物理合理性。