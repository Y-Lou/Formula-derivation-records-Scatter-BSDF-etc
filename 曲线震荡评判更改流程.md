# 曲线震荡评判更改流程

## 生成震荡数据

```python
import numpy as np
import matplotlib.pyplot as plt

# 设置随机种子以便结果可重复
np.random.seed(42)
# 生成 x 轴数据（例如 -5 到 5 之间的 500 个点）
x = np.linspace(-5, 5, 500)
# 定义抛物线趋势：y = a*(x - h)^2 + k
# 选择顶点在 (2, 1)，开口系数 0.3，得到一条向上弯曲的抛物线
a = 0.3
h = 2
k = 1
parabola_trend = a * (x - h)**2 + k
# 添加随机噪声（波动）
noise_amplitude = 0.5  # 噪声强度
random_noise = np.random.normal(0, noise_amplitude, size=x.shape)
# 合成带随机波动的曲线
y_random = parabola_trend + random_noise
# 可选：添加周期性波动（如正弦波）代替随机噪声，形成更规律的波动
freq = 7               # 波动频率
periodic_fluctuation = 0.4 * np.sin(freq * x)   # 正弦波动
y_periodic = parabola_trend + periodic_fluctuation
```

## **Fairness Energy（曲线公平性）**算法

### 生成二阶差分矩阵

```python
# 获取采样点数量
valueNum = len(y_periodic)
# 计算差分矩阵大小
matrixRow = valueNum-2
matrixCol = valueNum
CFMatrix = np.zeros(matrixRow,matrixCol)
# 创建二阶差分矩阵
firstIndex = 0
valueList = [1,-2,1]
for i in range(matrix):
    for j in range(3):
        CFMatrix[i][j+firstIndex] = valueList[j]
    firstIndex+=1
```

### 计算二阶差分

```python
# 计算离散曲率
cfValue = CFMatrix.dot(y_periodic.T)
```

### 计算离散的平均

```python
# 计算公平性能量（曲率的平方和）
E = 0
for i in range(len(cfValue)):
    E+=cfValue[i]**2
```

![1773725069649](../Desktop/1773725069649.png)

### 进行数据拟合

如果进行数据优化则需要
$$
L = min||y-y_d||^{2}+\lambda E
$$
求解优化：
$$
(I + \lambda D^{T}D)y = y_d
$$
详细推导：

原式展开
$$
L = (y-y_d)^{T}(y-y_d)+\lambda y^{T}D^{T}Dy
$$
因为要求$min(L)$，可将上式等价位一元二次函数，因此可等价为求$L$斜率为零对应的$y$

因此对上式求导
$$
\frac{\partial L}{\partial y} = 2(y-y_d)+2\lambda D^{T}Dy
$$
令导数为零得：
$$
2(y-y_d)+2\lambda D^{T}Dy = 0
\\
y-y_d+\lambda D^{T}Dy = 0
\\
y_d = y+\lambda D^{T}Dy
\\
y_d = (I+\lambda D^{T}D)y
$$

```python
# 最优解推导
# 通过评判L的大小来确定曲线是否平滑
# 需要提高lambda的权重来提高平滑判断，减小min||y-y_d||^2的影响
```

三阶差分衡量曲率的变化率，二阶差分衡量曲率

![1773284469220](assets/1773284469220.png)

可添加三阶差分为一个判断变量，但权重不易太高。

## 曲率频谱判断方法

### 基本流程

- 计算二阶差分（计算曲率）
- 对二阶差分进行FFT
- 计算高频能量的比例

二阶差分求FFT

```python
import numpy as np
# 调用np的fft接口进行计算
fftValue = np.fft(cfValue)
# 计算高频低频能量
# 按照随机分配的方式进行分段，每一段分别进行高频低频的能量
step = int(np.rand(0,1) * 10 + 10) # 取10-20个随机分段
stepLen = int(len(fftValue) / （step-1)) # 先计算平均采样数量
# 计算随机采样数量

```

计算高频能量占比

假设全频分段中的元素数为4，低频2个，高频2个。计算高低频能量
$$
E_{high} = C_{k3}^{2}+C_{k4}^{2}
\\
E_{low} = C_{k1}^{2}+C_{k2}^{2}
$$
计算能量占比
$$
R = \frac{E_{high}}{E_{high}+E_{low}}
$$

|    R    |   含义   |
| :-----: | :------: |
|  <0.1   | 曲线平滑 |
| 0.1~0.3 | 轻微震荡 |
|  >0.3   | 剧烈震荡 |

## FFT频谱判断

![1773725228529](../Desktop/1773725228529.png)

## 曲率评判与FFT组合检测噪声方法

上述两种方法进行组合检测。

![](assets/噪声判断1.png)

或者进行曲率噪声与频谱能量噪声的组合检测

但是要确定曲率噪声的数量级，不能远大于频谱噪声
$$
曲率噪声
\\
\rho_{k} = std(y_{i+1}-2y_{i}+y_{i-1})
\\
频谱能量比例
\\
\rho_{e} = \frac{E_{high}}{E_{high}+E_{low}}
\\
Score = \alpha\rho_{k}+\beta\rho_{e}
$$
当然频谱能量比例可使用两种方法进行求解：

- 直接对真实值做FFT，计算能量比例
- 先对真实值做二阶差分，然后对二阶差分做FFT，然后随机分段，每一段随机或平均选取高频低频能量，计算高频能量占比，累加进行评判。