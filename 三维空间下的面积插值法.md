# 直接使用三维坐标进行插值，而不显式降维到二维。核心思路是**在三维空间中利用三角形面积比**（通过叉积计算），实现共面四边形的灰度插值。
``` c++
#include <iostream>
#include <cmath>
#include <vector>

// 三维点，包含灰度值
struct Point3D {
    double x, y, z;
    double gray;

    Point3D(double x = 0, double y = 0, double z = 0, double gray = 0)
        : x(x), y(y), z(z), gray(gray) {}
};

// 向量运算
Point3D operator-(const Point3D& a, const Point3D& b) {
    return Point3D(a.x - b.x, a.y - b.y, a.z - b.z);
}

double dot(const Point3D& a, const Point3D& b) {
    return a.x * b.x + a.y * b.y + a.z * b.z;
}

Point3D cross(const Point3D& a, const Point3D& b) {
    return Point3D(
        a.y * b.z - a.z * b.y,
        a.z * b.x - a.x * b.z,
        a.x * b.y - a.y * b.x
    );
}

double norm(const Point3D& v) {
    return std::sqrt(v.x * v.x + v.y * v.y + v.z * v.z);
}

// 计算三角形面积（三维空间）
double triangleArea(const Point3D& a, const Point3D& b, const Point3D& c) {
    Point3D ab = b - a;
    Point3D ac = c - a;
    Point3D cp = cross(ab, ac);
    return 0.5 * norm(cp);
}

// 三角形重心坐标插值（直接使用三维坐标，基于面积比）
// 返回值：插值灰度值
// 要求：点P在三角形内部（包括边界）
double triangleInterpolate3D(const Point3D& p,
                             const Point3D& a, const Point3D& b, const Point3D& c) {
    double areaABC = triangleArea(a, b, c);
    if (areaABC < 1e-12) {
        // 退化三角形，返回顶点平均值
        return (a.gray + b.gray + c.gray) / 3.0;
    }

    // 子三角形面积
    double areaPBC = triangleArea(p, b, c);
    double areaAPC = triangleArea(a, p, c);
    double areaABP = triangleArea(a, b, p);

    // 重心坐标
    double alpha = areaPBC / areaABC;  // 对应顶点 A
    double beta  = areaAPC / areaABC;  // 对应顶点 B
    double gamma = areaABP / areaABC;  // 对应顶点 C

    return alpha * a.gray + beta * b.gray + gamma * c.gray;
}

// 判断点P是否在三角形ABC内（包括边界）
// 基于重心坐标法，检查三个子三角形面积之和是否等于原三角形面积，且每个子面积为非负
bool isPointInTriangle3D(const Point3D& p,
                         const Point3D& a, const Point3D& b, const Point3D& c) {
    double areaABC = triangleArea(a, b, c);
    if (areaABC < 1e-12) return false;

    double areaPBC = triangleArea(p, b, c);
    double areaAPC = triangleArea(a, p, c);
    double areaABP = triangleArea(a, b, p);

    // 允许数值误差
    const double eps = 1e-9;
    if (areaPBC < -eps || areaAPC < -eps || areaABP < -eps) return false;
    double sum = areaPBC + areaAPC + areaABP;
    return std::abs(sum - areaABC) < eps;
}

// 四边形插值（凸四边形，按对角线 AC 分割为两个三角形）
// 要求：四个点共面且构成凸四边形，点P也在同一平面内
double quadrilateralInterpolate3D(const Point3D& p,
                                  const Point3D& a, const Point3D& b,
                                  const Point3D& c, const Point3D& d) {
    if (isPointInTriangle3D(p, a, b, c)) {
        return triangleInterpolate3D(p, a, b, c);
    } else if (isPointInTriangle3D(p, a, c, d)) {
        return triangleInterpolate3D(p, a, c, d);
    } else {
        // 理论上凸四边形内的点必落在一个三角形中，但以防数值误差，返回四个顶点的平均
        std::cerr << "Warning: Point not clearly inside either triangle. Using average." << std::endl;
        return (a.gray + b.gray + c.gray + d.gray) / 4.0;
    }
}

// 示例使用
int main() {
    // 四个共面点（例如 z = 0 平面上的正方形）
    Point3D A(0, 0, 0, 100);
    Point3D B(2, 0, 0, 200);
    Point3D C(2, 2, 0, 300);
    Point3D D(0, 2, 0, 400);

    // 内部点 P(1, 1, 0) —— 理论灰度 250
    Point3D P(1, 1, 0, 0);
    double gray = quadrilateralInterpolate3D(P, A, B, C, D);
    std::cout << "Interpolated gray at (1,1,0): " << gray << std::endl;

    // 另一个点
    Point3D P2(0.5, 0.5, 0, 0);
    gray = quadrilateralInterpolate3D(P2, A, B, C, D);
    std::cout << "Interpolated gray at (0.5,0.5,0): " << gray << std::endl;

    // 非正方形凸四边形示例：梯形
    Point3D E(0, 0, 0, 100);
    Point3D F(3, 0, 0, 200);
    Point3D G(2, 2, 0, 300);
    Point3D H(1, 2, 0, 400);
    Point3D Q(1.5, 1, 0, 0);
    gray = quadrilateralInterpolate3D(Q, E, F, G, H);
    std::cout << "Interpolated gray at (1.5,1,0) in trapezoid: " << gray << std::endl;

    return 0;
}
```