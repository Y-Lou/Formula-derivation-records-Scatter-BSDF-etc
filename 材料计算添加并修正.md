# 材料计算添加并修正

## 皮纹材料计算修正

---

BSDF文件类型分为两种：**1.极角范围[0,180]、2.极角范围[-90,90]**。

立体角计算
$$
\begin{array}{l} 
\Omega = \int^{2\pi}_{0}\int^{\pi}_{0} sin{\theta}d\theta d\phi
\\
\Delta\Omega = \int^{\phi}_{0}\int^{\theta}_{0} sin{\theta}d\theta d\phi
\end{array} 
$$


### 类型一

---

使用SPEOS软件中的TypeCIES屏幕对皮纹进行仿真运算，制作对应的BSDF文件。其极角范围为[0,180]，并且因为是IES文件，所以范围边界会向外扩展半个步长。例如以1°为间隔，角度范围从0度到180度，数据矩阵中含有0度与180度对应的光强。数据排布具体如下图：

![1766054879198](C:\Users\yueshenl220241\Desktop\项目整改思路\assets\1766054879198.png)

因此积分角运算
$$
\begin{array}{l} 
\Delta\Omega = \int^{2\pi}_{0}\int^{\theta+\frac{1}{2}\Delta\theta}_{\theta-\frac{1}{2}\Delta\theta} sin{\theta}d\theta d\phi
\\
\Omega = (cos(\theta-\frac{1}{2}\Delta\theta) - cos(\theta+\frac{1}{2}\Delta\theta))\times \Delta\phi
\end{array} 
$$

### 类型二

---

使用SPEOS软件中的XMP屏幕对皮纹进行仿真运算，制作对应的BSDF文件。其极角范围为[-90,90]，并且因为是IES文件，所以范围边界会向外扩展半个步长。例如以1°为间隔，角度范围从-90度到90度，数据矩阵中不含有-90度与90度对应的光强。数据排布具体如下图：

![1766055780724](C:\Users\yueshenl220241\Desktop\项目整改思路\assets\1766055780724.png)

因此积分角运算
$$
\begin{array}{l} 
\Delta\Omega = \int^{2\pi}_{0}\int^{\theta+\Delta\theta}_{\theta} sin{\theta}d\theta d\phi
\\
\Omega = (sin(\theta+\Delta\theta) - sin(\theta))\times \Delta\phi
\end{array}
$$
计算好积分角后将$cd$转换为$lm$，从而进行概率矩阵运算。

BSDF文件可看作皮纹在特定角度下的能量分布，数据单位转换为$lm$后，也可看作接收光线的数量分布。因此在做出射角度计算时，可以将数据矩阵作为概率分布函数的各个位置的概率密度值，来计算各个范围的概率大小。然后通过均匀分布值来做随机选取，使得最终形成的能量分布与BSDF文件中保持一致。

具体操作如下：

1. 通过数据单位转换将BSDF数据转换为能量值

2. 对整个能量分布求和，计算总能量

3. 计算概率分布

    1. $$
        [\frac{\sum^{1}_{j}E_{j}}{\sum^{N}_{i}E_{i}},\frac{\sum^{2}_{j}E_{j}}{\sum^{N}_{i}E_{i}},\frac{\sum^{3}_{j}E_{j}}{\sum^{N}_{i}E_{i}},...,\frac{\sum^{N}_{j}E_{j}}{\sum^{N}_{i}E_{i}}]
        $$

    2. 上述解释对于$x$的概率密度函数为$f(x)$，$x$的概率为$F(x) = \int^{x}_{0}f(x)dx$，可通过数值积分进行近似。概率计算方法如上述方法。

4. 

5. 通过均匀分布对概率列表进行随机采样，获取对应的极角与水平角，从而使最终分布满足BSDF文件。

    1. 获取行列

        1. ```C++
            // index:概率列表索引值  Pro_Col:数据矩阵的列数
            Row = index / Pro_Cols;//数据列表行数，用于极角运算
            Col = index % Pro_Cols;//数据列表列数，用于水平角运算
            R = randFunInCells();//随机数 比率【0,1】
            DAD = AngleStep * DEG_TO_RAD; //数据步长 弧度制
            ```

    2. 极角计算
        $$
        \begin{array}{l} 
        Angle = Row \times DAD + \frac{DAD}{2}
        \\
        Polar = \arccos(\cos(\theta) - R \times (\cos(\theta) - cos(\theta + \Delta\theta)))
        \\
        BSDF_{XMP}数据极角计算
        \\
        Polar = \arccos(\cos(\theta - \frac{\Delta\theta}{2}) - R \times (\cos(\theta - \frac{\Delta\theta}{2}) - cos(\theta + \frac{\Delta\theta}{2})))
        \\
        BSDF_{TypeC}数据极角计算
        \\
        Polar = \arccos(\cos(\theta) - R \times (\cos(\theta) - cos(\theta + \Delta\theta)))
        \end{array} 
        $$

    3. 水平角计算
        $$
        Azimuth = DAD \times (Col  + R) + \frac{DAD}{2}
        $$

    4. 方向运算

        1. $$
	        \begin{array}{l} 
            z = \cos(Polar)
            \\
            xy = \sqrt{1-z\times z}
            \\
            Direction_x = xy\times \cos(Azimuth)
            \\
            Direction_y = xy\times \sin(Azimuth)
            \\
            Direction_z = z
            \end{array} 
            $$

以上为出射光线计算流程。
## 视觉仿真亮度计算
![[Pasted image 20260715093047.png]]
$$
	\begin{array}{l} 
x = r\sin\theta \cos \phi \\
y = r\sin \theta \sin \phi \\
z = r\cos \theta
\end{array} 
	
	$$
1. 计算光线入射角度
	1. 入光光线方向与入光点法向的夹角
2. 通过交互面的法向与入射光线的方向计算出光点坐标系
	1. Z轴-入光点法向
	2. Y轴-法向与入射光线方向的叉乘方向
	3. X轴-$Z\times Y$
3. 将出光方向旋转到出光点坐标系
4. 通过入射角与选择的皮纹数据矩阵的角度，通过折射定律或反射定律计算偏转角
5. 绕光线坐标系Y轴的方向旋转转出新的方向(简单的将极角绕哪个轴旋转向量就绕哪个轴旋转)
	1. ![[Pasted image 20260715094853.png]]
	2. ![[Pasted image 20260715094911.png]]
	3. ![[Pasted image 20260715094548.png]]
	4. ![[Pasted image 20260715094611.png]]
6. 依据新的出光方向计算极角与方位角
	1. $$
	\begin{array}{l} 
	\theta = \arccos (z)
	\\
	\phi = \arctan(abs(y) / abs(x))
	\end{array} 
	$$
	2. 因为CATIA计算出的轴X是镜像的因此$\phi$的计算需要将原有的顺时针旋转改为逆时针旋转
	3. 顺时针旋转角度计算

| **X** |     **+**      |     **-**      |     **-**      |     **+**      |
| :---: | :------------: | :------------: | :------------: | :------------: |
| **Y** |     **+**      |     **+**      |     **-**      |     **-**      |
|       | **$0\sim 90$** | **$180-\phi$** | **$\phi+180$** | **$360-\phi$** |
	  4.逆时针旋转角度计算
	

| **X** |     **-**      |     **-**      |     **+**      |     **+**      |
| :---: | :------------: | :------------: | :------------: | :------------: |
| **Y** |     **+**      |     **-**      |     **-**      |     **+**      |
|       | **$0\sim 90$** | **$180-\phi$** | **$\phi+180$** | **$360-\phi$** |

7. 依据新的角度查表计算
**注意：**
- 判断问题：
	- 使用投射皮纹时会有视角处于反射方向的情况，因此在做偏转角度计算时需要通过z轴的正负来进行评判
- 边界问题
	- 在大视角下，会出现透射光偏转至反射的角度上或者反射光偏转至投射的角度上，突破了数据表的边界，目前的解决方案
		- 在透射情况下，如果偏转后的极角小于90度，则直接取极角为90度加step
		- 在反射情况下，如果偏转后的极角大于等于90度，则极角取90度减step

### 角度偏转

```
AngleTemp;// BSDF入射角度数据
inputAngle;// 光线与入射平面的夹角
angleRotate;// 光线与入射平面的夹角与BSDF入射角度数据的偏转
outAX;// inputAngle折射角度
outA;// AngleTemp折射角度
angleRotateOut;// outAX与outA的差值
angleRotateC;// 光线与入射平面的夹角与BSDF入射角度数据的偏转
DirTemp.z();// 查表获取的极角偏转；大于0反射光；小于0透射光
VerX;// VerY与平面法向的叉乘方向
VerY;// 入射光与平面法向的叉乘方向
ioNormal;// 平面法向
```

目前的方案是先计算方向然后进行偏转，是否应该是先计算偏转角度然后影响查表得到的角度

#### 可能方案

---

需要在概率矩阵层面做插值计算

方法有两种(在相同的随机选择下)：

- 直接对行列值进行插值计算
- 对方向进行插值运算

## 薄膜材料添加

---



## 面属性高亮黑材料添加

---

读取文件

1. **SKM::File::ReadAsciiAsBinary(iFile, SFileText)** 作为二进制文件读取到字符串中

    1. ```c++
        bool File::ReadAsciiAsBinary(const char *iFilePath, SKM::String & oTextString)
        {
            oTextString.Release();//
            if ((NULL == iFilePath) || ("" == iFilePath))
            {
                return false;
            }
        
        
        
            size_t oSize = GetFileSize(iFilePath);//获得文件尺寸
            if ( 0 == oSize)
            {
                return false;
            }
        
            //打开文件
            FILE *pFile = NULL;
            fopen_s(&pFile,iFilePath, "rb");
            if (NULL == pFile)
            {
                return false;
            }	
            oTextString.ResizeNoCopy(oSize);//一次分配足够的空间。
        
            if (oSize != fread(oTextString.Tuple(), 1, oSize, pFile))
            {
                oTextString.Release();//
                fclose(pFile);//关闭文件
                return false;
            }
            fclose(pFile);//关闭文件
            return true;
        }
        ```

2. 按行进行分割

    1. ```c++
        SKM::ListOfcharPtr StrLinePtrList;//定义行字符串指针列表
        SKM::String::SplitLines(iFile, StrLinePtrList);//把字符串按照行进行拆分，会改变输入字符串
        ```

3. 数据解析

    1. ```c++
        /// <summary>按照不显示的字符（空白字符）进行分割</summary>
        /// <param name="iopString">字符串指针，非空，以‘\0’为结束标记，（会修改输入字符串）</param>
        /// <param name="oStringPtrList">输出子字符串指针列表</param>
        /// <remarks>一班是ASCII数值小于33的字符，去除头部空白字符</remarks>
        void String::SplitByNoShowChar (char * iopString, SKM::ListOfcharPtr & oStringPtrList)
        {
            oStringPtrList.ResizeZero();
            //是否为空
            if (NULL == iopString)
            {
                return;
            }
            if (0 ==  *iopString)//空字符串
            {
                return;
            }
            //定义上一个字符串
            char Lastchar=0;
        
            //循环查找空白符并替换为0
            while( 0 != *iopString)
            {
                if(*iopString<33)
                {
                    *iopString=0;//设为空
                    iopString++;
                    Lastchar=0;
                    continue;
                }
                if ( 0 ==Lastchar)
                {
                    oStringPtrList.Append();
                    oStringPtrList.Back()=iopString;
                }
                Lastchar=*iopString;
                iopString++;
            }
            //添加最后一个
            if (0 == Lastchar && 0 !=*iopString)
            {
                oStringPtrList.Append();
                oStringPtrList.Back()=iopString;
            }
            return;
        }
        ```

    2. 字符串转为浮点数 **SKM::atof(ValueStringList[i])**