# 自定义光源（数学推导） - 2025.10.16

## 朗伯光源

### 立体角（Ω）：

https://blog.csdn.net/fortunemouse/article/details/127854879

是一个物体对特定点的三维空间的角度。立体角是单位球面上的一块面积，这和“平面角是单位圆上的一段弧长”类似
$$
d\Omega = \frac{dA}{r^2}
=sin\theta d\theta d\phi
$$

### 朗伯光源建立

------

#### 理论介绍

假设朗伯光源最大半衰角为 $ \theta_{max} ​$，依据朗伯定律：
$$
I(\theta) = I_{0}\cos{\theta }
$$
$\theta​$ 为与法线的夹角，$I​$为光强。

因此，$I(\theta)​$ 与 $\cos{\theta }​$ 成正比：
$$
I(\theta)  \propto  \cos{\theta }
$$

若把发射方向当成一个随机变量 $\omega=(\theta,\varphi)​$，所以希望方向的概率密度函数$p​$也与$\cos{\theta }​$成正比：
$$
p(\omega)  \propto  \cos{\theta }，\omega \in \{0 \leq \theta \leq \theta_{max}, 0 \leq \phi \leq 2\pi \}
$$
$\theta = 2 / \pi​$时是完整的半空间。

而在球坐标系中，单位立体角微元表示为：$d \omega = \sin{\theta} d\theta d\phi​$

并且依据能量转换公式：$E = cd \ast d \omega = I(\theta) \ast \sin{\theta} d\theta d\phi$，因此：
$$
E(\theta) \propto  \sin{\theta} \cos{\theta }
$$
因此，朗伯光源光线方向的概率密度函数服从以下关系：
$$
p(\theta, \phi)  \propto  \sin{\theta} \cos{\theta }
$$

假设：
$$
p(\theta, \phi) = C \sin{\theta} \cos{\theta }
$$

#### 计算归一化常数

因为概率密度函数$p(\theta, \phi)$的积分应该为1，因此，对$p(\theta, \phi)$积分：

$$
\int_{0}^{2\pi} \int_{0}^{\theta_{max}} p(\theta, \phi) d\theta d\phi = \int_{0}^{2\pi} \int_{0}^{\theta_{max}} C \sin{\theta} \cos{\theta } d\theta d\phi
$$

$$
= 2\pi C \int_{0}^{\theta_{max}} \sin{\theta} \cos{\theta } d\theta
$$

$$
=2\pi C \int_{0}^{\theta_{max}} \sin{\theta} d \sin{\theta}
$$

$$
=2\pi C \cdot \frac{1}{2} \sin^{2}{\theta}\Big|_{0}^{\theta_{max}}
$$

$$
=C \pi\sin^{2}{\theta_{max}} = 1
$$

所以：
$$
C = \frac{1}{\pi\sin^{2}{\theta_{max}}}
$$
因此：
$$
p(\theta, \phi) = \frac{\sin{\theta} \cos{\theta }}{\pi\sin^{2}{\theta_{max}}}
$$

#### 采样计算

根据计算获取的归一化常数，对朗伯光源光线方向的概率密度函数进行积分运算：
$$
\int_{0}^{2\pi} \int_{0}^{\theta} p(\theta, \phi) d\theta d\phi = \int_{0}^{2\pi} \int_{0}^{\theta} \frac{\sin{\theta} \cos{\theta }}{\pi\sin^{2}{\theta_{max}}} d\theta d\phi
$$

$$
=\int_{0}^{\theta} \frac{2 \cdot \sin{\theta} \cos{\theta }}{\sin^{2}{\theta_{max}}} d\theta
$$

$$
=\frac{2}{\sin^{2}{\theta_{max}}} \int_{0}^{\theta} \sin{\theta} \cos{\theta } d\theta
$$

$$
=\frac{2 \cdot \frac{1}{2} \cdot \sin^{2}{\theta}}{\sin^{2}{\theta_{max}}}
$$

$$
= \frac{\sin^{2}{\theta}}{\sin^{2}{\theta_{max}}}
$$

因此累计分布函数$F(\theta, \phi)$为：
$$
F(\theta, \phi) = \frac{\sin^{2}{\theta}}{\sin^{2}{\theta_{max}}}
$$

#### 概率采样

依据朗伯光源的特性，累计分布函数$F(\theta, \phi)​$，方向满足$u \sim U(0,1)​$的均匀分布，因此：
$$
u = F(\theta, \phi) = \frac{\sin^{2}{\theta}}{\sin^{2}{\theta_{max}}}
$$
其中$\theta $为极角，满足$0 \sim \frac{\pi}{2}$。$\phi$为水平角，满足$0 \sim 2\pi$，并且可以通过均匀分布进行随机采样。$\theta_{max}$为设置好的半衰角，规定朗伯光源的扩散角大小。
因为$u = F(\theta, \phi)​$，所以：
$$
\sin^{2}{\theta} = u \cdot \sin^{2}{\theta_{max}}
$$
$$
\sin{\theta} = \sqrt{u} \cdot \sin{\theta_{max}}
$$
根据单位向量计算方向：
$$
r = \sin{\theta} = \sqrt{u} \cdot \sin{\theta_{max}}
$$

$$
(x,y,z) = (r \cdot \cos{\phi}, r \cdot \sin{\phi}, \sqrt{1 - r^{2}})
$$

其中$\phi = u_2 \cdot 2\pi​$。$(u \sim U(0,1) )​$
关于能量，郎伯光源每根光线的能量为：$E_{i} = \frac{E}{N}​$

## Cos（余弦）分布光源

### 理论介绍

强度随出射角 $\theta​$（相对于法向）满足余弦衰减的幂律，常用于近似漫射/朗伯类或可以通过调整幂次控制汇聚/发散的“叶片式”分布。

常用数学表达（阶数 n）
$$
I(\theta) = I_{0}\cos^{n}{\theta }，  \{0 \leq \theta \leq \pi/2 \}
$$
依据上一节郎伯光源的方向计算思路，概率密度函数也需要满足：
$$
p(\theta, \phi) \propto  \cos^{n}{\theta }\sin{\theta}
$$
因此，假设：
$$
p(\theta, \phi) = C \cos^{n}{\theta }\sin{\theta}
$$


### 计算归一化常数

因为概率密度函数$p(\theta, \phi)​$的积分应该为1，因此，对概率密度函数（光强对立体角积分）积分：
$$
\int_{0}^{2\pi} \int_{0}^{\theta_{max}} p(\theta, \phi) d\theta d\phi = \int_{0}^{2\pi} \int_{0}^{\theta_{max}} C \cos^{n}{\theta } \sin{\theta} d\theta d\phi
$$

$$
= 2\pi C \int_{0}^{\theta_{max}} \cos^{n}{\theta } \sin{\theta } d\theta
$$

$$
= 2\pi C \int_{0}^{\theta_{max}} -\cos^{n}{\theta } d\cos{\theta}
$$

置换 $u=\cos{⁡\theta}$,当$⁡\theta$从 $⁡\theta \sim \theta_{max}$时 $u$ 从 $\cos{\theta_{max}} \sim 0$，且$du = - d\cos{\theta} = -\sin \theta d \theta$。于是积分变为：
$$
\pi C \int_{\cos{\theta_{max}}}^{1} u^{n} du = 2\pi C (\frac{u^{n+1}}{n+1} \big|_{\cos \theta_{max}}^{1})
$$
所以：
$$
1 = 2\pi C \cdot \frac{1-\cos^{n+1}_{\theta_{max}}}{n+1}
$$

$$
C = \frac{n+1}{2\pi (1-\cos^{n+1}_{\theta_{max}})}
$$

### 采样计算

根据计算获取的归一化常数，对$\cos​$光源光线方向的概率密度函数进行积分运算：
$$
F(\theta, \phi) = \int_{0}^{2\pi} \int_{0}^{\theta} \frac{n+1}{2\pi (1-\cos^{n+1}_{\theta_{max}})} \cos^{n}{\theta } \sin{\theta} d\theta d\phi
$$

$$
= \frac{n+1}{(1-\cos^{n+1}_{\theta_{max}})}\int_{0}^{\theta}  \cos^{n}{\theta } \sin{\theta} d\theta
$$

$$
= \frac{n+1}{(1-\cos^{n+1}_{\theta_{max}})} \int_{\theta}^{0}  \cos^{n}{\theta }  d\cos{\theta}
$$

$$
= \frac{n+1}{(1-\cos^{n+1}_{\theta_{max}})} \int_{\cos{\theta}}^{1} u^{n} du
$$

$$
= \frac{n+1}{(1-\cos^{n+1}_{\theta_{max}})} \frac{(1-\cos^{n+1}_{\theta})}{n+1}
$$

$$
= \frac{(1-\cos^{n+1}_{\theta})}{(1-\cos^{n+1}_{\theta_{max}})}
$$

累计分布函数$F(\theta, \phi)$，方向满足$u \sim U(0,1)$的均匀分布，因此：
$$
u = \frac{(1-\cos^{n+1}_{\theta})}{(1-\cos^{n+1}_{\theta_{max}})}
$$
所以：
$$
\cos^{n+1}{\theta} = 1 - u(1-\cos^{n+1}_{\theta_{max}})
$$

$$
\cos{\theta} =  \sqrt[n+1]{1 - u(1-\cos^{n+1}_{\theta_{max}})}
$$

### 概率采样

根据单位向量计算方向：
$$
z = \cos{\theta} = \sqrt[n+1]{1 - u((1-\cos^{n+1}_{\theta_{max}}))}
$$

$$
r = \sqrt{1 - z^{2}}
$$

$$
(x,y,z) = (r \cdot \cos{\phi}, r \cdot \sin{\phi}, z)
$$

其中$\phi = u_2 \cdot 2\pi$。$(u \sim U(0,1) )$
关于能量，郎伯光源每根光线的能量为：$E_{i} = \frac{E}{N}$

## 对称高斯光源 (Symmetric Gaussian)

### 理论介绍

在角空间（以极角$\theta​$ 为径向）延径向呈径向对称的高斯强度分布，适合描述开关/聚光透镜或近似圆形发光体的发散角分布。

**常用的数学表达式：**

以角度$\theta$为径向角度
$$
I(\theta) = I_{0}\exp(-\frac{\theta^{2}}{2 \sigma^{2}})
$$
这里的$\sigma$ 是角空间的标准差(弧度制)，分布与方位角$\phi$无关，因为径向对称。

因此
$$
I(\theta)  \propto exp(-\frac{\theta^{2}}{2 \sigma^{2}})
$$
方向的概率密度函数也需要满足：
$$
p(\theta, \phi)  \propto  \exp(-\frac{\theta^{2}}{2 \sigma^{2}}) \sin \theta
$$
因此，假设：
$$
p(\theta, \phi) = C \exp(-\frac{\theta^{2}}{2 \sigma^{2}})\sin{\theta}
$$

#### 计算归一化常数

因为概率密度函数$p(\theta, \phi)$的积分应该为1，因此，对于$p(\theta, \phi)​$的积分为：
$$
\int_{0}^{2\pi} \int_{0}^{\theta_{max}}  C \exp(-\frac{\theta^{2}}{2 \sigma^{2}}) \sin{\theta} d\theta d\phi = 1
$$
因为没有简单的初等闭式，因此使用**小角近似——当**$\sigma$**很小时**($\theta\ll 1$**弧度**)

取$\sin{\theta}\approx \theta$，上限可扩展到$\infty $**(因为高斯远端贡献很小)**，则：
$$
\int_{0}^{2\pi} \int_{0}^{\theta_{max}} p(\theta, \phi) d \theta d \phi = \int_{0}^{2\pi} \int_{0}^{\theta_{max} }  C \exp(-\frac{\theta^{2}}{2 \sigma^{2}}) \theta d\theta d\phi
$$

$$
= 2\pi C \int_{0}^{\theta_{max} } \exp(-\frac{\theta^{2}}{2 \sigma^{2}}) \theta d\theta
$$

令$u = \frac{\theta^{2}}{2 \sigma^{2}}$,则$\theta d\theta = \sigma^{2}du$,因此上式可化为：
$$
\int_{0}^{2\pi} \int_{0}^{\theta_{max}} p(\theta, \phi) = 2 \pi C \sigma^{2} \int_{0}^{\frac{\theta^{2}_{max}}{2\sigma^{2}} } \exp(-u) du
$$

$$
= - 2 \pi C \sigma^{2} \int_{0}^{-\frac{\theta^{2}_{max}}{2\sigma^{2}}} \exp(u) du
$$

$$
= 2 \pi C \sigma^{2} \int_{-\frac{\theta^{2}_{max}}{2\sigma^{2}}}^{0} \exp(u) du
$$

$$
= 2 \pi C \sigma^{2} (\exp(0) - \exp(- \frac{\theta^{2}_{max}}{2\sigma^{2}}))
$$

$$
= 2 \pi C \sigma^{2}(1-\exp(- \frac{\theta^{2}_{max}}{2\sigma^{2}}))
$$

因为$\int_{0}^{2\pi} \int_{0}^{\theta_{max}} p(\theta, \phi) = 1​$所以小角近似下的归一化常数为：
$$
C = \frac{1}{2\pi \sigma^{2}(1-\exp(- \frac{\theta^{2}_{max}}{2\sigma^{2}}))}
$$

#### **FWHM**（半高全宽，Full Width at Half Maximum）

FWHM 定义为在曲线高度降到峰值一半处的宽度，即$ I(\theta_{1/2}) = \frac{I_0}{2}  ​$



即$\theta = \pm \frac{FWHM}{2}​$时有$I(\theta) = \frac{I_{0}}{2}​$。

带入高斯表达式：
$$
\frac{I_{0}}{2} = I_{0}\exp{-\frac{(\theta_{1/2})^{2}}{2\sigma^{2}}}
$$
对两边取对数
$$
\ln{\frac{1}{2}} = -\frac{(\theta_{1/2})^{2}}{2\sigma^{2}}
\\
\Rightarrow \theta_{1/2} = \sigma \sqrt{2 \ln2}
$$
因为FWHM是全宽度，即从$-\theta_{1/2}$到$+ \theta_{1/2}​$，所以：
$$
FWHM = 2 \theta_{1/2} = 2\sigma\sqrt{2\ln{2}}
\\
\Rightarrow \sigma = \frac{FWHM}{2 \sqrt{2\ln2}}
$$
**对于各项异性的高斯分布**
$$
FWHM_{x} = 2 \sigma_{x} \sqrt{2\ln{2}}
\\
FWHM_{y} = 2 \sigma_{y} \sqrt{2\ln{2}}
$$

### 采样计算

根据计算获取的归一化常数，对**Symmetric Gaussian**光源光线方向的概率密度函数进行积分运算：
$$
\int_{0}^{2\pi} \int_{0}^{\theta} p(\theta, \phi) d\theta d\phi = 
\int_{0}^{2\pi} \int_{0}^{\theta }  \frac{1}{2\pi \sigma^{2}(1-\exp(- \frac{\theta^{2}_{max}}{2\sigma^{2}}))} \exp(-\frac{\theta^{2}}{2 \sigma^{2}}) \theta d\theta d\phi
\\
= 2 \pi \frac{1}{2\pi \sigma^{2}(1-\exp(- \frac{\theta^{2}_{max}}{2\sigma^{2}}))} \sigma^{2} \int_{0}^{\frac{\theta^{2}}{2\sigma^{2}} } \exp(-u) du
\\
= \frac{1}{(1-\exp(- \frac{\theta^{2}_{max}}{2\sigma^{2}}))}  \int_{-\frac{\theta^{2}}{2\sigma^{2}} }^{0} \exp(u) du
\\
=\frac{1}{(1-\exp(- \frac{\theta^{2}_{max}}{2\sigma^{2}}))} (\exp(0) - \exp(- \frac{\theta^{2}}{2\sigma^{2}}))
\\
= \frac{1}{(1-\exp(- \frac{\theta^{2}_{max}}{2\sigma^{2}}))}(1-\exp(- \frac{\theta^{2}}{2\sigma^{2}}))
$$
所以：
$$
F(\theta,\phi) = \frac{1}{(1-\exp(- \frac{\theta^{2}_{max}}{2\sigma^{2}}))}(1-\exp(- \frac{\theta^{2}}{2\sigma^{2}}))
$$

### 概率采样

依据**Symmetric Gaussian**光源的特性，概率函数$F(\theta, \phi)$，方向满足$u \sim U(0,1)$的均匀分布，因此：
$$
u = F(\theta,\phi) = \frac{1}{(1-\exp(- \frac{\theta^{2}_{max}}{2\sigma^{2}}))}(1-\exp(- \frac{\theta^{2}}{2\sigma^{2}}))
$$
所以：
$$
u(1-\exp(-\frac{\theta_{max}^{2}}{2\sigma^{2}})) = 1 - \exp(-\frac{\theta^{2}}{2\sigma^{2}})
\\
\exp(-\frac{\theta^{2}}{2\sigma^{2}}) = 1-u(1-\exp(-\frac{\theta_{max}^{2}}{2\sigma^{2}}))
$$
两边同事求对数$\ln(\exp)$
$$
-\frac{\theta^{2}}{2\sigma^{2}} = \ln(1-u(1-\exp(-\frac{\theta_{max}^{2}}{2\sigma^{2}})))
\\
\theta^{2} = -2\sigma^{2}\ln(1-u(1-\exp(-\frac{\theta_{max}^{2}}{2\sigma^{2}})))
$$
根据单位向量计算方向：
$$
r = \sin{\theta}
$$

$$
(x,y,z) = (r \cdot \cos{\phi}, r \cdot \sin{\phi}, \sqrt{1 - r^{2}})
$$

其中$\phi = u_2 \cdot 2\pi​$。$(u \sim U(0,1) )​$
关于能量，郎伯光源每根光线的能量为：$E_{i} = \frac{E}{N}​$

## 非对称/椭圆 高斯光源（Asymmetric Gaussian）

