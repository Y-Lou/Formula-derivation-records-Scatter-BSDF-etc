# 皮纹插值计算方案

## 半球线性插值

半球线性插值**主要用于处理与光源方向相关的数据，特别是在涉及**半球空间的方向时。光源分布通常是依赖于入射光的方向，而半球线性插值则用于估计在半球上某个方向之间的光强度变化。同理，**BSDF**文件一样。

### 应用方向

1. **光源分布的插值**

   半球线性插值可以用来在离散采样点之间估算光源的方向分布。假设，光源的亮度或辐射强度在某些方向上已经计算出来，那么在其他方向上，半球线性插值可以帮助估计未采样点的光强。

2. **BRDF插值**

   BRDF的模拟往往需要考虑物体表面的反射特性，而这些反射特性在球面上具有方向性。通过半球线性插值，可以在离散的反射角度（例如入射角和出射角）之间进行插值，计算表面在不同角度下的反射强度。

### 优势

- **效率**
- **平滑**

### 计算原理

首先获取离散数据，不同入射角光线对应的出射能量分布$E(\theta_i,\phi_j)$，在球面坐标系下。其中，$\theta$表示极角，$\phi$表示水平角。

进行数据插值，选择两个已知方向 $(\theta_1, \phi_1)$ 和 $(\theta_2, \phi_2)$，它们之间有已知的光强度值 $I(\theta_1, \phi_1)$ 和 $I(\theta_2, \phi_2)$。假设目标方向为$(\theta, \phi)$，通过线性插值估算的光强$I(\theta,\phi)$:
$$
I(\theta, \phi) = I(\theta_1, \phi_1) \cdot \frac{d_2}{d_1 + d_2} + I(\theta_2, \phi_2) \cdot \frac{d_1}{d_1 + d_2}
$$
其中：$d_1$ 和 $d_2$ 分别是目标方向 $(\theta, \phi)$ 到已知方向 $(\theta_1, \phi_1)$ 和 $(\theta_2, \phi_2)$ 的距离。

因此在距离的计算上，将$R$设置为一，采用**4点插值法**更加稳妥。

## 立体角插值

立体角插值关注的是基于物体或光源的视角分布，通过**立体角**来描述空间中某一方向的视野范围。立体角是一个三维角度度量，表示从一个点看向一个表面区域的角度大小。

**常用情况：**

1. 给定一些离散的方向数据（例如，某些入射角度上的反射亮度），通过插值得到其他方向上的数据。
2. 通过立体角权重来调整不同方向上的数据，以得到准确的光照分布或反射亮度。

### 优势

- **适应性强：**可处理大部分的分布情况
- **更加精准：**通过考虑每个方向的立体角，插值结果可以更加准确地反映实际分布。

### 计算步骤

1. **采样数据获取**

2. **计算立体角权重：**

   立体角可以通过球面坐标系中的角度$(\theta, \phi)$来计算，球面上每个微小区域的立体角 $\Delta \Omega$ 可以通过以下公式计算：
   $$
   \Delta \Omega = \sin \theta \Delta \theta  \Delta \phi
   其中，\Delta \theta 和 \Delta \phi是角度的变化量，\theta是极角，\phi是方位角。
   $$
   其中，$\Delta \theta$ 和 $\Delta \phi$ 是角度的变化量，$\theta$ 是极角，$\phi$ 是方位角。

   3.**立体角加权插值：**
$$
I(\theta,\phi) = \frac{\Omega_1 I(\theta_1, \phi_1) + \Omega_2 I(\theta_2, \phi_2)}{\Omega_1 + \Omega_2}
$$
​	其中，$\Omega_1$ 和 $\Omega_2$ 是两个方向的立体角。

   4.**4点插值**取周围四个角度进行插值计算，计算最终结果。**(双线性插值)**

### 优缺点

- 可以精准的描述光源或反射模型中与立体角相关的现象，适用于光源和物体表面角度分布的插值。
- 计算相对复杂，尤其是对于高维数据和大规模的采样数据，**需要考虑多种角度的权重。**

### 提升方案

- 与**高阶插值和多项式插值**结合

- 通过**BSDF模型**进行选择

- **球面线性插值（Spherical Linear Interpolation, SLERP）**

  将极角$\theta​$与水平角$\phi​$转换为方向向量$\mathbf{v}​$进行线性插值，可以在两个方向之间进行平滑的插值。其公式为：
  $$
  \mathbf{v}_{\text{interp}} = \mathbf{v}_1 \cdot \sin((1 - t) \cdot \theta) + \mathbf{v}_2 \cdot \sin(t \cdot \theta)
  \\
  \mathbf{v}_{\text{interp}} = \frac{\mathbf{v}_1 \cdot \sin((1 - t) \cdot \theta)}{\sin{\theta}} + \frac{\mathbf{v}_2 \cdot \sin(t \cdot \theta)}{\sin{\theta}}
  $$
  上边两个公式需要进行尝试，确定正确性。

  其中：$\mathbf{v}_{\text{interp}}$最终插值方向，$\mathbf{v}_1$和 $\mathbf{v}_2$ 是球面上的两个方向，$\theta$ 是它们之间的夹角，$t$ 是插值因子。

  $\mathbf{v}$转换到笛卡尔坐标系表示为：
  $$
  x = \sin(\theta) \cos(\phi), \quad y = \sin(\theta) \sin(\phi), \quad z = \cos(\theta)
  $$
  转换为三维空间中的单位向量 $\mathbf{v} = (x, y, z)$

  夹角$\theta$计算，$\theta = \cos^{-1}(\mathbf{v}_1 \cdot \mathbf{v}_2) $。

  方向向量的插值计算转换为光强的插值计算：
  $$
  f_{\text{interp}}(\theta, \phi) = \left( \frac{\sin((1 - t)\theta)}{\sin \theta} f(\theta_1, \phi_1) \right) + \left( \frac{\sin(t\theta)}{\sin \theta} f(\theta_2, \phi_2) \right)
  $$
  其中，$f(\theta_1, \phi_1)$ 和 $f(\theta_2, \phi_2)$ 分别是两个数据点对应的BSDF值，$f_{\text{interp}}(\theta, \phi)$ 是插值结果。

  **插值步骤猜想**

  先进行一个方向的插值计算，然后在进行另一个方向的插值计算。比如，先进行水平角方向的插值运算，然后依据新的插值数据进行极角的插值运算，得到最终的BSDF数据。

  **优点：**更加平滑，适应性强

  **缺点：**计算量大，对数据分布要求较高

  **$t$插值因子计算**

  - **均匀步长**

  - **根据角度差计算**
    $$
    t = \frac{\theta - \theta_1}{\theta_2 - \theta_1}
    $$
    可以保证根据角度范围进行变化

#### 通过蒙特卡洛积分法计算出偏转角度后的角度偏转

现有的方案：

**极点$180^\circ$：**
$$
\theta = \pi
\\
R = Rand[0\sim 1]
\\
\phi = 2\pi  \times  R
$$
**Other：**
$$
R = Rand[0\sim 1]
\\
Angle = FindRow \times AngleStep
\\
\theta = \arccos{(\cos{\theta_i} - R \times (\cos{\theta_i} - \cos{\theta_{i+1}}))}
\\
\phi = R \times FindCol
$$
改进方案：

是否可以通过随机值来决定插值因子$t$，通过球面线性插值计算角度间隔之间随机角度偏转。

## 傅里叶插值

# 入射角度间的插值运算

现有的方案，使用的是实测角度的角度偏转，认为中间值与实测值大致一致，只需要做相应的角度偏转即可。后续可通过**插值**进行更小间隔的角度偏转。通过**球面线性插值**计算。