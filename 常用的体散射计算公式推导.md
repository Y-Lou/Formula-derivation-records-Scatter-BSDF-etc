# Material Scatter HenyeyGreenstein
$$
原本的HG相函数为:
\\
p_{HG}=\frac{1}{4\pi} \frac{1-g^{2}}{(1+g^{2}-2*g*\cos{\theta})^{\frac{3}{2}}}
\\
$$

通过极坐标转换，将上式乘以立体角$2\pi\sin\theta​$得

$$
\begin{array}{l} 
\int_{0}^{\pi}p_{HG}*2\pi\sin\theta d\theta = \int_{0}^{\pi}\frac{(1-g^{2})}{4\pi(1+g^{2}-2*g*\cos{\theta})^{\frac{3}{2}}}*2\pi\sin\theta d\theta = 1
\\[10pt]
\int_{-1}^{1}\frac{(1-g^{2})}{4\pi(1+g^{2}-2*g*\cos{\theta})^{\frac{3}{2}}}*2\pi d\cos\theta = 1
\\[10pt]
设\cos\theta = \mu
\\[10pt]
\int_{-1}^{1}\frac{(1-g^{2})}{4\pi(1+g^{2}-2*g*\mu)^{\frac{3}{2}}}*2\pi d\mu = 1
\\[10pt]
等式变换
\\[10pt]
\int\frac{(1-g^{2})}{2(1+g^{2}-2*g*\mu)^{\frac{3}{2}}} d\mu = 1
\\[10pt]
因此
\\[10pt]
p(\mu) = \frac{(1-g^{2})}{2(1+g^{2}-2*g*\mu)^{\frac{3}{2}}} = p(cos\theta) = \frac{(1-g^{2})}{2(1+g^{2}-2*g*\cos\theta)^{\frac{3}{2}}}
\end{array}
\tag{H-0}
$$

**Aniosotropy Factor(g)**

$$
p_{HG}=\frac{1}{4\pi} \frac{1-g^{2}}{(1+g^{2}-2*g*\cos{\theta})^{\frac{3}{2}}}
\tag{H-0.1}
$$
**偏向角$\theta​$的抽样由方向余弦$\mu = \cos{\theta}​$的几率密度函数确定**

对于Henyey-Greenstein相函数

$PHG$实际上是关于方向的概率分布函数，转换到极坐标后可表示为$p(cos\theta)$,但是其本质上是关于亮度的概率密度函数，需要乘以立体角$2\pi sin\theta$才能彻底转换成关于角度$\theta$的概率密度函数
$$
p(\cos{\theta}) =\frac{(1-g^{2})}{2(1+g^{2}-2*g*\cos{\theta})^{\frac{3}{2}}}
\tag{H-0.2}
$$
**步长与散射方向的概率抽样**

**1.概率抽样**

首先设定随机变量$x​$，可以代表步长的$s​$或散射偏转角度$\theta​$，再引入几率密度函数$p(x)​$,定义为
$$
\int_{a}^{b}p(x)dx = 1, a\leq x \leq b
\tag{H-1}
$$
那么随机变量$x​$落在区间$[a,x_{1}]​$内的几率分布函数为
$$
F_{x}(x_{1}) = \int_{a}^{x_{1}}p(x)dx
\tag{H-2}
$$
若将计算机产生的伪随机数$\xi​$看成随机变量$x​$，则它的几率分布是均匀的，即几率密度函数为单位矩形分布$p(\xi)=1​$，而且$\xi \in [0,1]​$,于是相应的几率分布函数为
$$
F_{x}(\xi) =\int_{0}^{\xi_{1}}p(\xi)d \xi = \xi_{1}
\tag{H-3}
$$
或令
$$
F_{\xi}(\xi) = F_{x}(x)
\tag{H-4}
$$
即
$$
\xi = \int_{0}^{x}p(x)dx
\tag{H-5}
$$
因此只要知道$p(x)​$或$F(x)​$的形式，就可以得出随机变量$x​$的分布采样。

**2.步长的采样**

按吸收和散射系数定义，在$[s_{1},s_{1}+ds_{1}]​$内每单位长度发生吸收或者散射的几率为 
$$
\mu_{t} = \frac{-dp\left\{ s\ge s_{1} \right \}}{p\left\{ s\ge s_{1} \right \}ds_{1}}
\tag{H-6}
$$
其中$p\left\{ s\ge s_{1} \right \}$代表步长大于$s_{1}$时发生吸收或者散射的几率。

利用$p\left\{ s\ge 0 \right \}=1$得
$$
p\left\{ s < s_{1} \right \} =1-p\left\{ s\ge s_{1} \right \}
\\ 
=1-exp(-\mu_{t}s_{1})
\tag{H-7}
$$
因此，步长的几率密度函数为
$$
p(s_{1}) = \frac{dp \left\{s<s_{1} \right \}}{ds_{1}} = \mu_{t}exp(-\mu_{t}s_{1})
\tag{H-8}
$$
由式$(3),(5)$得
$$
\xi = \int p(s)ds = 1-exp(-\mu_{t}s_{1})
\tag{H-9}
$$
得步长为
$$
s_{1} = \frac{-In(1-\xi)}{\mu_{t}} = \frac{-In(\xi)}{\mu_{t}}
\tag{H-10}
$$
$(10)$式为无限介质或半无限介质中光子运动步长的采样。

**3.散射方向的采样**

注意到光的散射是沿着入射方向轴对称的，若采用极坐标，需要同时用散射偏向角$\theta \in [0,\pi]$ 和方位角$\varphi = [0,2\pi]$来确定一个具体的方向

偏向角$\theta​$的采样可由方向余弦$\mu = \cos \theta​$的概率密度函数来确定，对于**Henyey-Greenstein**相函数
$$
p(\cos{\theta}) =\frac{(1-g^{2})}{2(1+g^{2}-2*g*\cos{\theta})^{\frac{3}{2}}}
\tag{H-11}
$$
其中$g$为平均散射余弦，应用式$(3),(5)$，得
$$
\xi = \int^{\mu}_{-1}p(\mu)d\mu = \int^{\mu}_{-1}\frac{1-g^{2}}{2(1+g^{2}-2g\mu)^{\frac{3}{2}}}d\mu
\tag{H-12}
$$
**整体过程如下：**
$$
\xi = \int^{\mu}_{-1}p(\mu)d\mu = \int^{\mu}_{-1}\frac{1-g^{2}}{2(1+g^{2}-2g\mu)^{\frac{3}{2}}}d\mu
\tag{H-12.1}
$$

$$
\frac{2\xi}{1-g^{2}} = \int^{\mu}_{-1}\frac{1}{(1+g^{2}-2g\mu)^{\frac{3}{2}}}d\mu
\tag{H-12.2}
$$

设$(1+g^{2}-2g\mu) = \Psi (\mu)​$，因此：
$$
\frac{2\xi}{1-g^{2}} = \int^{\mu}_{-1} \frac{1}{\Psi(\mu)^{\frac{3}{2}}} d\mu
\tag{H-12.3}
$$
等式转换.
$$
\frac{2\xi}{1-g^{2}} = -\frac{1}{2g}\int^{\mu}_{-1} \frac{1}{\Psi(\mu)^{\frac{3}{2}}} d\Psi(\mu)
\tag{H-12.4}
$$

$$
\frac{-4\xi g}{1-g^{2}} = \int^{\mu}_{-1} \frac{1}{\Psi(\mu)^{\frac{3}{2}}}d\Psi(\mu)
\tag{H-12.5}
$$

$$
\frac{-4\xi g}{1-g^{2}} = -2\int^{\mu}_{-1}d\Psi(\mu)^{-\frac{1}{2}}
\tag{H-12.6}
$$

$$
\frac{2\xi g}{1-g^{2}} =\int^{\mu}_{-1}d\Psi(\mu)^{-\frac{1}{2}}
\tag{H-12.7}
$$

$$
\frac{2\xi g}{1-g^{2}} = \Psi(\mu)^{-\frac{1}{2}} |^{\mu}_{-1}
\tag{H-12.8}
$$

$$
\frac{2\xi g}{1-g^{2}} = (1+g^{2}-2g\mu)^{-\frac{1}{2}} - (1+g^{2}+2g\mu)^{-\frac{1}{2}}
\tag{H-12.9}
$$

$$
\frac{2\xi g}{(1-g)(1+g)}+\frac{1}{(1+g)^{2*\frac{1}{2}}} = \frac{1}{(1+g^{2}-2g\mu) ^{\frac{1}{2}}}
\tag{H-12.10}
$$

$$
\frac{2\xi g}{(1-g)(1+g)}+\frac{1}{1+g} = \frac{2\xi g + 1-g}{(1+g)(1-g)} =\frac{1-g+2\xi g}{1-g^{2}}=\frac{1}{(1+g^{2}-2g\mu) ^{\frac{1}{2}}}
\tag{H-12.11}
$$

$$
(1+g^{2}-2g\mu)^{\frac{1}{2}} = \frac{1-g^{2}}{1-g+2g\xi}
\tag{H-12.12}
$$

$$
2g\mu = 1+g^{2} - (\frac{1-g^{2}}{1-g+2g\xi})^{2}
\tag{H-12.13}
$$

从中解得：
$$
\mu = \frac{1}{2g}[1+g^{2}-(\frac{1-g^{2}}{1-g+2g\xi})^{2}]
\tag{H-13}
$$
$\xi​$为随机数$[0\sim1]​$

$$
\theta = \arccos(\mu)
\tag{H-14}
$$
角度能量分布计算公式
$$
E = \frac{(1-g^{2})}{(1+g^{2}-2*g*\cos{\theta})^{\frac{3}{2}}}
\tag{H-15}
$$
归一化计算
$$
E_{i}^{'} = \frac{E_{i}}{E_{0}}
\tag{H-16}
$$

---

g值计算
$$
\begin{array}{l} 
R_{1i} = E*\cos{\theta{i}}*\sin{\theta_{i}}
\\[10pt]
R_{2i} = E*\sin{\theta_{i}}
\\[10pt]
g = \frac{\sum^{0}_{i}R_{1i}}{\sum^{0}_{i}R_{2i}}
\end{array}
\tag{H-17}
$$

---

# Double Henyey-Greenstein

**$P_{DHG}​$相函数，其中$\mu=\cos\theta​$
$$
P_{DHG} = \frac{1}{4\pi}(\frac{(1-\alpha)(1-g_{1}^{2})}{(1-2g_{1}\mu +g_{1}^{2})^{\frac{3}{2}}}+\frac{\alpha(1-g_{2}^{2})}{(1 - 2g_{2}\mu +g_{2}^{2})^{\frac{3}{2}}})
\tag{D-1}
$$

通过极坐标转换，将上式乘以立体角$2\pi\sin\theta​$得
$$
\begin{array}{l} 
\int_{0}^{\pi} p_{DHG} * 2\pi\sin\theta d\theta 
= \int_{0}^{\pi} \frac{1}{4\pi} (\frac{(1-\alpha)(1-g_{1}^{2})}{(1-2g_{1}\cos\theta +g_{1}^{2})^{\frac{3}{2}}}+\frac{\alpha(1-g_{2}^{2})}{(1 - 2g_{2}\cos\theta +g_{2}^{2})^{\frac{3}{2}}}) * 2\pi\sin\theta d\theta 
= 1
\\[10pt]
\int_{-1}^{1} \frac{1}{2} (\frac{(1-\alpha)(1-g_{1}^{2})}{(1-2g_{1}\cos\theta +g_{1}^{2})^{\frac{3}{2}}}+\frac{\alpha(1-g_{2}^{2})}{(1 - 2g_{2}\cos\theta +g_{2}^{2})^{\frac{3}{2}}}) d\cos\theta
= 1
\end{array} 

\tag{D-2}
$$
由于$\cos\theta = \mu$​
所以：
$$
\int \frac{1}{2} (\frac{(1-\alpha)(1-g_{1}^{2})}{(1-2g_{1}\mu +g_{1}^{2})^{\frac{3}{2}}}+\frac{\alpha(1-g_{2}^{2})}{(1 - 2g_{2}\mu +g_{2}^{2})^{\frac{3}{2}}}) d\mu
= 1
\tag{D-3}
$$
因此：
$$
p(\mu) = \int \frac{1}{2} (\frac{(1-\alpha)(1-g_{1}^{2})}{(1-2g_{1}\mu +g_{1}^{2})^{\frac{3}{2}}}+\frac{\alpha(1-g_{2}^{2})}{(1-2g_{2}\mu +g_{2}^{2})^{\frac{3}{2}}}) d\mu

\tag{D-4}
$$
其中$g$为平均散射余弦，应用式$(H-3),(H-5)$，得
$$
\xi = \int^{\mu}_{-1}p(\mu)d\mu
= \int_{-1}^{\mu} \frac{1}{2} (\frac{(1-\alpha)(1-g_{1}^{2})}{(1-2g_{1}\mu +g_{1}^{2})^{\frac{3}{2}}}+\frac{\alpha(1-g_{2}^{2})}{(1-2g_{2}\mu +g_{2}^{2})^{\frac{3}{2}}}) d\mu

\tag{D-5}
$$
整体积分如下：
$$
\begin{array}{l} 
2\xi = \int_{-1}^{\mu} \frac{(1-\alpha)(1-g_{1}^{2})}{(1-2g_{1}\mu +g_{1}^{2})^{\frac{3}{2}}}+\frac{\alpha(1-g_{2}^{2})}{(1-2g_{2}\mu +g_{2}^{2})^{\frac{3}{2}}} d\mu
\\[10pt]
2\xi = \int_{-1}^{\mu} \frac{(1-\alpha)(1-g_{1}^{2})}{(1-2g_{1}\mu +g_{1}^{2})^{\frac{3}{2}}} d\mu 
+ \int_{-1}^{\mu}\frac{\alpha(1-g_{2}^{2})}{(1-2g_{2}\mu +g_{2}^{2})^{\frac{3}{2}}} d\mu
\end{array}
\tag{D-6}
$$
先对等式右侧的左式积分：
$$
\begin{array}{l} 
\int_{-1}^{\mu} \frac{(1-\alpha)(1-g_{1}^{2})}{(1-2g_{1}\mu +g_{1}^{2})^{\frac{3}{2}}} d\mu

= (1-\alpha)(1-g_{1}^{2}) \int_{-1}^{\mu} (1-2g_{1}\mu +g_{1}^{2})^{-\frac{3}{2}} d\mu
\\[10pt]
令t = 1-2g_{1}\mu +g_{1}^{2}
\\[10pt]
则dt = -2g_1 d\mu \rightarrow d\mu = -\frac{1}{2g_1}dt
\\[10pt]
代入上式得：

\\[10pt]
-\frac{(1-\alpha)(1-g_{1}^{2})}{2g_1} \int_{(1+g_1)^2}^{1-2g_1\mu+g_1^2} t^{-\frac{3}{2}} dt
\\[10pt]
= -\frac{(1-\alpha)(1-g_{1}^{2})}{2g_1} * (-2 * t^{-1/2}) | ^{1-2g_1\mu+g_1^2}_{(1+g_1)^2}
\\[10pt]
= \frac{(1-\alpha)(1-g_{1}^{2})}{g_1} ( t^{-1/2}) | ^{1-2g_1\mu+g_1^2}_{(1+g_1)^2}
\\[10pt]
= \frac{(1-\alpha)(1-g_{1}^{2})}{g_1} *[(1-2g_1\mu+g_1^2)^{-1/2} - \frac{1}{1+g_1}]
\\[10pt]
\frac{(1-\alpha)}{g_1} * [(\frac{1 - g_1^2}{\sqrt{1-2g_1\mu+g_1^2}}) - (1-g_1)]
\end{array}
\tag{D-7}
$$
再对等式右侧的右式积分：
$$
\begin{array}{l} 
\int_{-1}^{\mu}\frac{\alpha(1-g_{2}^{2})}{(1-2g_{2}\mu +g_{2}^{2})^{\frac{3}{2}}} d\mu
= \alpha(1-g_{2}^{2}) \int_{-1}^{\mu}(1-2g_{2}\mu +g_{2}^{2})^{-\frac{3}{2}} d\mu
\\[10pt]
令t = 1-2g_{2}\mu +g_{2}^{2}
\\[10pt]
则dt = -2g_{2} d\mu \rightarrow d\mu = -\frac{1}{2g_{2}} dt
\\[10pt]
代入上式得：
\\[10pt]
F(t) = -\frac{\alpha(1-g_{2}^{2})}{2g_{2}} \int_{(1+g_2)^2}^{1 - 2g_2\mu +g_2^2} t^{-3/2}dt
\\[10pt]
= -\frac{\alpha(1-g_{2}^{2})}{2g_{2}} * (-2 * t^{-1/2}) | ^{1-2g_2\mu+g_2^2}_{(1+g_2)^2}
\\[10pt]
= \frac{\alpha(1 - g_{2}^{2})}{g_{2}} (t^{-1/2}) | ^{1-2g_2\mu+g_2^2}_{(1+g_2)^2}
\\[10pt]
= \frac{\alpha(1 - g_{2}^{2})}{g_{2}} [(1-2g_2\mu+g_2^2)^{-1/2} - \frac{1}{1+g_2}]
\\[10pt]
= \frac{\alpha}{g_2} * [(\frac{1-g_2^2}{\sqrt{1-2g_2\mu+g_2^2}}) - (1-g_2)]
\end{array}
\tag{D-8}
$$
将$(D-7),(D-8)带入(D-6)​$：
$$
\begin{array}{l} 
2\xi = \frac{(1-\alpha)}{g_1} * [(\frac{1 - g_1^2}{\sqrt{1-2g_1\mu+g_1^2}}) - (1-g_1)] + \frac{\alpha}{g_2} * [(\frac{1-g_2^2}{\sqrt{1-2g_2\mu+g_2^2}}) - (1-g_2)]
\\[10pt]
\xi = \frac{(1-\alpha)}{2g_1} * [(\frac{1 - g_1^2}{\sqrt{1-2g_1\mu+g_1^2}}) - (1-g_1)] + \frac{\alpha}{2g_2} * [(\frac{1-g_2^2}{\sqrt{1-2g_2\mu+g_2^2}}) - (1-g_2)]
\end{array}
\tag{D-9}
$$
一般情况下，无法写出$\mu = f(\xi)$的显示表达式，只能通过牛顿公式法或二分法进行数值求解

## 牛顿公式法计算流程，以求解$x^2 - 2 = 0$求解$x$为例

流程代码如下

```c++
#include <iostream>
#include <cmath>
#include <functional>
#include <stdexcept>

/**
 * 二分法缩小根区间
 * @param f     目标函数
 * @param a     区间左端点
 * @param b     区间右端点
 * @param tol   区间宽度容差（当 b-a < tol 时停止二分）
 * @param max_iter 最大二分迭代次数
 * @return      缩小后的区间中点
 * @throws std::runtime_error 若区间不含根或达到最大迭代次数仍未满足要求
 */
double bisectionShrink(const std::function<double(double)>& f,
                       double a, double b,
                       double tol = 1e-3, int max_iter = 50) {
    double fa = f(a), fb = f(b);
    if (fa * fb >= 0) {
        throw std::runtime_error("二分法失败：区间端点函数值同号，无法保证有根。");
    }

    double mid = 0.0;
    for (int iter = 0; iter < max_iter; ++iter) {
        mid = (a + b) / 2.0;
        double fmid = f(mid);

        if (fmid == 0.0 || (b - a) / 2.0 < tol) {
            return mid;
        }

        if (fa * fmid < 0) {
            b = mid;
            fb = fmid;
        } else {
            a = mid;
            fa = fmid;
        }
    }
    // 返回当前中点，即使未达到绝对容差
    return (a + b) / 2.0;
}

/**
 * 牛顿法（带安全性检查）
 * @param f         目标函数
 * @param df        导数函数
 * @param x0        初始猜测
 * @param tol       绝对误差容差
 * @param max_iter  最大迭代次数
 * @return          近似根
 * @throws std::runtime_error 导数过小或迭代不收敛
 */
double newtonRaphson(const std::function<double(double)>& f,
                     const std::function<double(double)>& df,
                     double x0, double tol = 1e-12, int max_iter = 50) {
    double x = x0;
    for (int iter = 0; iter < max_iter; ++iter) {
        double fx = f(x);
        double dfx = df(x);

        if (std::abs(dfx) < 1e-12) {
            throw std::runtime_error("牛顿法失败：导数过小。");
        }

        double x_new = x - fx / dfx;
        if (std::abs(x_new - x) < tol) {
            return x_new;
        }
        x = x_new;
    }
    throw std::runtime_error("牛顿法失败：达到最大迭代次数。");
}

/**
 * 混合方法：二分法缩小根区间 + 牛顿法高精度求解
 * @param f            目标函数
 * @param df           导数函数
 * @param a            初始区间左端点
 * @param b            初始区间右端点
 * @param bisect_tol   二分法停止的区间宽度容差
 * @param newton_tol   牛顿法停止的绝对误差容差
 * @return             高精度近似根
 */
double hybridRootFinder(const std::function<double(double)>& f,
                        const std::function<double(double)>& df,
                        double a, double b,
                        double bisect_tol = 1e-3,
                        double newton_tol = 1e-12) {
    // 第一步：二分法收缩区间
    double x0 = bisectionShrink(f, a, b, bisect_tol);
    // 第二步：牛顿法精确求解
    double root = newtonRaphson(f, df, x0, newton_tol);
    return root;
}

// 示例：求解 x^2 - 2 = 0 的正根
int main() {
    // 定义函数 f(x) = x^2 - 2
    auto f = [](double x) -> double { return x * x - 2.0; };
    // 定义导数 f'(x) = 2x
    auto df = [](double x) -> double { return 2.0 * x; };

    double left = 0.0, right = 2.0;   // 已知根在 [0, 2] 内
    try {
        double root = hybridRootFinder(f, df, left, right);
        std::cout.precision(15);
        std::cout << "方程的根为: " << root << std::endl;
        std::cout << "f(根) = " << f(root) << std::endl;
        std::cout << "与 sqrt(2) 的误差: " << std::abs(root - std::sqrt(2.0)) << std::endl;
    } catch (const std::exception& e) {
        std::cerr << "错误: " << e.what() << std::endl;
        return 1;
    }
    return 0;
}
```

### 求导计算

**原式为：**
$$
\xi = \frac{(1-\alpha)}{2g_1} * [(\frac{1 - g_1^2}{\sqrt{1-2g_1\mu+g_1^2}}) - (1-g_1)] + \frac{\alpha}{2g_2} * [(\frac{1-g_2^2}{\sqrt{1-2g_2\mu+g_2^2}}) - (1-g_2)]
$$
**转换为$f(\mu) = 0$**
$$
\begin{array}{l} 
0 = \frac{(1-\alpha)}{2g_1} * [(\frac{1 - g_1^2}{\sqrt{1-2g_1\mu+g_1^2}}) - (1-g_1)] + \frac{\alpha}{2g_2} * [(\frac{1-g_2^2}{\sqrt{1-2g_2\mu+g_2^2}}) - (1-g_2)]-\xi
\\[10pt]
所以:
\\[10pt]
f(\mu) = \frac{(1-\alpha)}{2g_1} * [(\frac{1 - g_1^2}{\sqrt{1-2g_1\mu+g_1^2}}) - (1-g_1)] + \frac{\alpha}{2g_2} * [(\frac{1-g_2^2}{\sqrt{1-2g_2\mu+g_2^2}}) - (1-g_2)]-\xi
\end{array}
$$
**求解$f^{'}(\mu)$**

设$t_{1}(\mu) = 1-2g_{1}\mu + g_{1}^{2}, t_{2}(\mu) = 1-2g_{2}\mu + g_{2}^{2}​$，因此原式可化为：
$$
0=\frac{(1-\alpha)}{2g_1} * [(\frac{1 - g_1^2}{\sqrt{t_{1}(\mu)}}) - (1-g_1)] + \frac{\alpha}{2g_2} * [(\frac{1-g_2^2}{\sqrt{t_{2}(\mu)}}) - (1-g_2)]-\xi
$$
根据求导法则
$$
f^{'}(\mu) = f^{'}(t_{1}(\mu))*t^{'}_{1}(\mu)+f^{'}(t_{2}(\mu))*t^{'}_{2}(\mu)
$$

$$
f^{'}(\mu) = \frac{1-\alpha}{2g_1} \cdot (1-g^{2}_{1})\cdot (-\frac{1}{2})\cdot t^{-\frac{3}{2}}_{1}(\mu)\cdot(-2g_1) + \frac{\alpha}{2g_2}\cdot (1-g^{2}_{2})\cdot (-\frac{1}{2})\cdot t^{-\frac{3}{2}}_{2}(\mu)\cdot(-2g_2)
$$

**最终的导数为：**
$$
f^{'}(\mu) = \frac{(1-\alpha)\cdot(1-g^{2}_{1})}{2(1-2g_{1}\mu + g^{2}_{1})^{\frac{3}{2}}} + \frac{\alpha \cdot (1-g^{2}_{2})}{2(1-2g_{2}\mu +g^{2}_{2})^{\frac{3}{2}}}
$$

-------------

# Material Scatter Henyey-Greenstein-Gegenbauer

**$P_{HG-G}​$相函数，其中$\mu=\cos \theta​$
$$
P_{HG-G} = \frac{ \alpha g(1-g^{2})^{2\alpha}}{\pi\cdot [(1+g)^{2\alpha}-(1-g)^{2\alpha}](1+g^{2}-2g\mu)^{(1+\alpha)}}
\tag{G-0}
$$


因为参数$g​$和$\alpha​$均为外部设定，所以$P_{HG-G}​$相函数可转换为：
$$
\begin{array}{l}
K = \frac{g\alpha(1-g^{2})^{2\alpha}}{((1+g)^{2\alpha}-(1-g)^{2\alpha})}
\\[10pt]
P_{HG-G} = \frac{K}{\pi(1+g^{2}-2g\mu)^{\alpha+1}}
\end{array}
\tag{G-1}
$$



**计算$p(\cos\theta) \Leftrightarrow p(\mu)​$**
$$
\int_{0}^{\pi}P_{HG-G}*2\pi\sin\theta d\theta = \int_{0}^{\pi}\frac{K}{\pi(1+g^{2}-2g\cos \theta)^{\alpha+1}}*2\pi\sin\theta d\theta = 1
\tag{G-2}
$$




$$
\int_{0}^{\pi}\frac{K}{\pi(1+g^{2}-2g\cos \theta)^{\alpha+1}}*2\pi\sin\theta d\theta = 1
\tag{G-2.1}
$$



$$
\int^{-1}_{1} -\frac{2K}{(1+g^{2}-2g\cos \theta)^{\alpha +1}}d\cos \theta = 1
\tag{G-3}
$$



$$
\int^{1}_{-1}\frac{2K}{(1+g^{2}-2g\cos \theta)^{\alpha +1}}d\cos \theta = 1
\tag{G-4}
$$

**所以$p(\cos\theta) \Leftrightarrow p(\mu)​$为：**
$$
p(\cos \theta) =\frac{2K}{(1+g^{2}-2g\cos \theta)^{\alpha +1}}
\tag{G-5}
$$



$$
p(\mu) = \frac{2K}{(1+g^{2}-2g\mu)^{\alpha +1}}
\tag{G-6}
$$


**散射方向的采样**

注意到光的散射是沿着入射方向轴对称的，若采用极坐标，需要同时用散射偏向角$\theta \in [0,\pi]​$ 和方位角$\varphi = [0,2\pi]​$来确定一个具体的方向

偏向角$\theta$的采样可由方向余弦$\mu = \cos \theta$的概率密度函数来确定，对于**Henyey-Greenstein-Gegenbauer**相函数转换得到的概率密度函数：
$$
p(\cos \theta) =\frac{2K}{(1+g^{2}-2g\cos \theta)^{\alpha +1}}
\tag{G-7}
$$


其中$g与\mu​$为特定参数，应用式$(H-3),(H-5)​$，得
$$
\xi = \int^{\mu}_{-1}p(\mu)d\mu = \int^{\mu}_{-1}\frac{2K}{(1+g^{2}-2g\mu)^{\alpha +1}}d\mu
\tag{G-8}
$$


**整体积分过程如下：**
$$
\xi = \int^{\mu}_{-1}p(\mu)d\mu = \int^{\mu}_{-1}\frac{2K}{(1+g^{2}-2g\mu)^{\alpha +1}}d\mu
\tag{G-9}
$$



$$
\frac{\xi}{2K} = \int^{\mu}_{-1} \frac{1}{(1+g^{2}-2g\mu)^{\alpha+1}}d\mu
\tag{G-10}
$$


设$(1+g^{2}-2g\mu) = \Psi (\mu)$，因此：
$$
\frac{\xi}{2K} = \int^{\mu}_{-1} \frac{1}{\Psi(\mu)^{\alpha+1}}d\mu
\tag{G-11}
$$



$$
\frac{\xi}{2K} = -\frac{1}{2g}*(-\frac{1}{\alpha})\int^{\mu}_{-1}d\Psi(\mu)^{-\alpha}
\tag{G-12}
$$



$$
\frac{g\xi \alpha}{K} = \int^{\mu}_{-1} d\Psi(\mu)^{-\alpha}
\tag{G-13}
$$



$$
\frac{g\xi \alpha}{K} = \Psi(\mu)^{-\alpha}|^{\mu}_{-1}
\tag{G-14}
$$



$$
\frac{g\xi \alpha}{K} = (1+g^{2}-2g\mu)^{-\alpha} - (1+g^{2}+2g)^{-\alpha}
\tag{G-15}
$$



$$
(1+g^{2}-2g\mu)^{-\alpha} = \frac{g\xi \alpha}{K} +(1+g^{2}+2g)^{-\alpha}
\tag{G-16}
$$



$$
\frac{1}{(1+g^{2}-2g\mu)^{\alpha}} = \frac{g\xi \alpha}{K} + \frac{1}{(1+g^{2}+2g)^{\alpha}}
\tag{G-17}
$$



$$
\frac{1}{(1+g^{2}-2g\mu)^{\alpha}} = \frac{g\xi \alpha(1+g^{2}+2g)^{\alpha} + K}{K(1+g^{2}+2g)^{\alpha}}
\tag{G-18}
$$



$$
(1+g^{2}-2g\mu)^{\alpha} = \frac{K(1+g^{2}+2g)^{\alpha}}{g\xi \alpha(1+g^{2}+2g)^{\alpha}+K}
\tag{G-19}
$$



$$
2g\mu = 1+g^{2}-\sqrt[\alpha]{\frac{K(1+g^{2}+2g)^{\alpha}}{g\xi \alpha(1+g^{2}+2g)^{\alpha}+K}}
\tag{G-20}
$$



$$
\mu = \frac{1}{2g}(1+g^{2}-\sqrt[\alpha]{\frac{K(1+g^{2}+2g)^{\alpha}}{g\xi \alpha(1+g^{2}+2g)^{\alpha}+K}})
\tag{G-21}
$$


因为$K = \frac{g\alpha(1-g^{2})^{2\alpha}}{((1+g)^{2\alpha}-(1-g)^{2\alpha})}$

所以等式可转化为：
$$
\mu = \frac{1}{2g}(1+g^{2} - \sqrt[\alpha]{\frac{\frac{g\alpha(1-g^{2})^{2\alpha}}{((1+g)^{2\alpha}-(1-g)^{2\alpha})}(1+g^{2}+2g)^{\alpha}}{g\xi \alpha(1+g^{2}+2g)^{\alpha}+\frac{g\alpha(1-g^{2})^{2\alpha}}{((1+g)^{2\alpha}-(1-g)^{2\alpha})}}})
\tag{G-22}
$$


$\xi为随机数[0\sim1]​$
$$
\theta = \arccos(\mu)
\tag{G-23}
$$


角度能量分布运算公式
$$
E = \frac{1-g^{2}}{(1+g^{2}-2*g*\cos{\theta})^{\alpha +1}}
\tag{G-24}
$$


归一化计算
$$
E_{i}^{'} = \frac{E_{i}}{E_{0}}
\tag{G-25}
$$

-----

# Isotropic scattering

**$P_{I}​$**相函数为：
$$
P_I = \frac{1}{4\cdot \pi}
\tag{I-1}
$$

将上式乘以立体角$2\pi\sin\theta​$得
$$
\begin{array}{l} 
\int_{0}^{\pi} \frac{1}{4\cdot \pi} * 2 \pi sin\theta d\theta = 1
\\[10pt]
\int_{-1}^{1} \frac{1}{2} * d cos\theta = 1
\\[10pt]
设\cos\theta = \mu
\\[10pt]
\int_{-1}^{1} \frac{1}{2} * d \mu = 1
\\[10pt]
因此
\\[10pt]
\int\frac{1}{2} * d \mu = 1
\\[10pt]
p(\mu) = \frac{1}{2}
\end{array}
\tag{I-2}
$$
因此相函数转换得到的概率密度函数为：
$$
p(\mu) = \frac{1}{2}
\tag{I-3}
$$
应用式$(H-3),(H-5)​$，得
$$
\xi = \int^{\mu}_{-1}p(\mu)d\mu = \int^{\mu}_{-1} \frac{1}{2} d\mu
\tag{I-4}
$$

$$
\begin{array}{l} 
\xi = \frac{1}{2} \mu |^{\mu}_{-1} = \frac{\mu}{2} + \frac{1}{2}
\\[10pt]
2\xi = \mu + 1
\\[10pt]

\mu = 2\xi - 1
\end{array}
$$

$$
因为：cos \theta = 2 \xi - 1
\\
所以：
\theta = arccos(2\xi - 1)
$$

$\xi为随机数[0\sim1]$

--------

# Rayleigh scattering

**$P_R​$**相函数，$\mu = \cos\theta​$
$$
P_R = \frac{3}{16\pi}(1+\cos^{2}\theta)
\tag{R-1}
$$
**计算$p(\cos\theta) \Leftrightarrow p(\mu)$**
$$
\int_{0}^{\pi}P_{R}*2\pi\sin\theta d\theta = \int_{0}^{\pi}\frac{3}{16\pi}(1+\cos^{2}\theta)*2\pi\sin\theta d\theta = 1
\tag{R-2}
$$

$$
\int_{0}^{\pi}\frac{3}{8}(1+\cos^{2}\theta)\sin\theta d\theta = 1
\tag{R-3}
$$

$$
-\frac{3}{8}\int^{-1}_{1}(1+cos^{2}\theta) d\cos\theta = 1
\tag{R-4}
$$

$$
\frac{3}{8}\int^{1}_{-1}(1+cos^{2}\theta)d\cos\theta=1
\tag{R-5}
$$

**所以$p(\cos\theta) \Leftrightarrow p(\mu)$为：**
$$
p(\mu) = \frac{3}{8}(1+\mu^{2})
\tag{R-6}
$$
**散射方向的采样**

注意到光的散射是沿着入射方向轴对称的，若采用极坐标，需要同时用散射偏向角$\theta \in [0,\pi]​$ 和方位角$\varphi = [0,2\pi]​$来确定一个具体的方向

偏向角$\theta$的采样可由方向余弦$\mu = \cos \theta$的概率密度函数来确定，对于**Rayleigh scattering**相函数转换得到的概率密度函数：
$$
p(\mu) = \frac{3}{8}(1+\mu^{2})
\tag{R-7}
$$
应用式$(H-3),(H-5)$，得:
$$
\xi = \int^{\mu}_{-1}p(\mu)d\mu = \int^{\mu}_{-1}\frac{3}{8}(1+\mu^{2})d\mu
\tag{R-8}
$$
**整体积分过程如下**
$$
\xi = \int^{\mu}_{-1}\frac{3}{8}(1+\mu^{2})d\mu
\tag{R-9}
$$

$$
\frac{8\xi}{3} = \int^{\mu}_{-1}d\mu + \int^{\mu}_{-1}\mu^{2}d\mu
\tag{R-10}
$$

$$
\frac{8\xi}{3} = \mu-(-1)+\int^{\mu}_{-1}\mu^{2}d\mu
\tag{R-11}
$$

$$
\frac{8\xi}{3}-(\mu+1) = \int^{\mu}_{-1}\mu^{2}d\mu
\tag{R-12}
$$

$$
\frac{8\xi-3\mu-3}{3} = \frac{1}{3}\int^{\mu^{3}}_{-1}d\mu^{3}
\tag{R-13}
$$

$$
\frac{8\xi-3\mu-3}{3} = \frac{1}{3}\mu^{3}|^{\mu}_{-1}
\tag{R-14}
$$

$$
8\xi-3\mu-3 = \mu^{3}+1
\tag{R-15}
$$

$$
8\xi-4 = \mu^{3}+3\mu
\tag{R-16}
$$

**将$8\xi -4$转化为$C$，则上式可化为$\mu^{3}+3\mu-C = 0$**

**使用卡尔丹公式求解$\mu$，设$\mu=m-\frac{1}{m}$**
$$
\mu^{3}= (m-\frac{1}{m})^{3} = (m+\frac{1}{m})(m+\frac{1}{m})^{2} = (m+\frac{1}{m})(m^{2}+\frac{1}{m^{2}}-2) = (m^{3}-2m+\frac{1}{m}-m+\frac{2}{m}-\frac{1}{m^{3}}) = m^{3}-\frac{1}{m^{3}}-3m+\frac{3}{m}
\tag{R-17}
$$

$$
\mu^{3} = m^{3}-\frac{1}{m^{3}}-3m+\frac{3}{m}
\tag{R-18}
$$

$$
3\mu = 3m-\frac{3}{m}
\tag{R-19}
$$

$(R18)+(R19)$得：
$$
\mu^{3}+3\mu = m^{3}-\frac{1}{m^{3}}
\tag{R-20}
$$
则原式可化为：
$$
m^{3}-\frac{1}{m^{3}}-C = 0
\tag{R-21}
$$
设$m^{3} = q$则：
$$
q-\frac{1}{q}-C = 0
\tag{R-22}
$$

$$
q^{2}-Cq-1 = 0
\tag{R-23}
$$

通过二次方程求解公式得：
$$
q = \frac{C\pm \sqrt{C^{2}+4}}{2}
\tag{R-24}
$$

$$
m^{3} =\frac{C\pm \sqrt{C^{2}+4}}{2}
\tag{R-25}
$$

$$
m = \sqrt[3]{\frac{C\pm \sqrt{C^{2}+4}}{2}}
\tag{R-26}
$$

$$
m =\sqrt[3]{\frac{(8\xi -4)\pm \sqrt{(8\xi -4)^{2}+4}}{2}}
\tag{R-27}
$$

因为$\mu = m-\frac{1}{m} = \frac{m^{2}-1}{m}​$
$$
\mu = \frac{(\frac{(8\xi -4)\pm \sqrt{(8\xi -4)^{2}+4}}{2})^{\frac{2}{3}}-1}{(\frac{(8\xi -4)\pm \sqrt{(8\xi -4)^{2}+4}}{2})^{\frac{1}{3}}}
\tag{R-28}
$$


$\xi为随机数[0\sim1]$
$$
\theta = \arccos(\mu)
\tag{R-29}
$$

# 上述方法伪代码计算如下

```c++
//线性插值
double ScatterIntrpolation(SKM::ListOfDouble ListYRate, SKM::ListOfDouble ListXRate,double iXrate)
{
    const size_t listSize = ListYRate.Size()
    //判断数量
    switch (listSize)
    {
    case 0:
        /* code */
        return 0;
    case 1:
        return ListYRate.Front();
    }
    if (iXRate<=ListXRate.Front()) return ListYRate.Front();
    if (iXRate>=ListXRate.Front()) return ListYRate.Back();
    //相对位置
    double * px = ListXRate.Tuple();
    size_t IndexEndFind = 1;//下标位置
    for (size_t i = 1; i < listSize; i++)
    {
        px++;
        if (iXrate <= *px)
        {
            IndexEndFind = i;//找到了
            break;
        }
        
    }
    //线性插值运算
    double x1 = *(px-1);
    double x2 = *px;
    if (x1 == x2) return ListYRate[IndexEndFind-1];
    double PosInSubList = (iXrate - x1)/(x2 - x1);
    double *py = ListYRate.Tuple() + IndexEndFind - 1;
    return *py + (*(py+1) - *py) * PosInSubList;
}

double HGFunction(double RayWave)
{
    double g = 0.0;
    //参数计算
    if (G.Size() == 1)
    {
        g = G.Front();
    }
    else
    {
        g = ScatterIntrpolation(this.G, this.WaveList, Ray.Wave);
    }
    // 需要添加错误判断
    try
    {
        double randomValue = GetRandom();
        double v1 = 1 + g * g;
        double v2 = 1 - g * g;
        double v3 = 1 - g + 2 * g * randomValue;
        return ArcCos((v1 - (v2 / v3) * (v2 / v3)) / (2 * g));
    }
    catch(const std::exception& e)
    {
        std::cerr << e.what() << '\n';
    }
    return 0.0;  
}

double HGGFunction(double RayWave)
{
    double g = 0.0;
    double alpha = 0.0;
    if (G.Size() == 1)
    {
        g = G.Front();
        alpha = Alpha.Front();
    }
    else
    {
        g = ScatterIntrpolation(this.G, this.WaveList, Ray.Wave);
        alpha = ScatterIntrpolation(this.Alpha, this.WaveList, Ray.Wave);
    }
    // 需要添加错误判断
    try
    {
        double randomValue = GetRandom();
        double K = Pow(g,alpha) / (Pow((1-g), -2*alpha) - Pow((g+1),-2*alpha));
        double v1 = 1+g*g;
        double v2 = K*Pow((1+g*g+2*g),alpha);
        double v3 = g*randomValue*alpha*Pow((1+g*g+2*g),alpha) + K;
        double v4 = Sqrt(v2/v3,alpha);
        return ArcCos((v1-v4)/(2*g));
    }
    catch(const std::exception& e)
    {
        std::cerr << e.what() << '\n';
    }
    return 0.0; 
}

double DHGFunction(double RayWave)
{
    double g1 = 0.0;
    double g2 = 0.0;
    double alpha = 0.0;
    if (G.Size() == 1)
    {
        g1 = DG1.Front();
        g2 = DG2.Front();
        alpha = Alpha.Front();
    }
    else
    {
        g1 = ScatterIntrpolation(this.DG1, this.WaveList, Ray.Wave);
        g2 = ScatterIntrpolation(this.DG2, this.WaveList, Ray.Wave);
        alpha = ScatterIntrpolation(this.Alpha, this.WaveList, Ray.Wave);
    }
    //牛顿公式法运算
    try
    {
		/*
		code
		*/
    }
    catch(const std::exception& e)
    {
        std::cerr << e.what() << '\n';
    }
    return 0.0; 
}

double IsotropicFunction()
{
    double randomValue = GetRandom();//获取随机数
    return ArcCos(2 * randomValue - 1);
}

double RayleighFunction()
{
    try
    {
        double randomValue = GetRandom();//获取随机数
        double v1 = 8 * randomValue - 4;
        double V2 = Sqrt(v1*v1+4);
        double v3 = Sqrt((v1+v2)/2,3);//情况一
        double v4 = Sqrt((v1-v2)/2,3);//情况二
        double mu1 = (v3*v3 - 1) / v3;
        double mu2 = (v4*v4 - 1) / v4;
        //结果评判
        if (mu1>=0 && mu2<=0)
        {
            return ArcCos(mu1);
        }
        else if (mu2>=0 && mu1<=0)
        {
            return ArcCos(mu2);
        }
        else if (mu2>=0 && mu1>=0)
        {
            double chooseValue = GetRandom();
            if (chooseValue>=0.5)
            {
                return ArcCos(mu2);
            }
            else
            {
                return ArcCos(mu1);
            }
        }
        else//计算错误
        {
            return 0.0;
        }
    }
    catch(const std::exception& e)
    {
        std::cerr << e.what() << '\n';
    }
    return 0.0;
}
```

