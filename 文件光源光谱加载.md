# 现有的方案
---
## 光源文件中本身带有光谱信息
---
- 加载光源数据
	- 能量都为1
	- 每根光线的波长都不一致
- 读取波长范围$[\lambda_{min},\lambda_{max}]$
- 根据波长数目对波长范围进行划分
- 根据光源数据计算波长范围列表中每个小范围的能量总和，然后进行归一化到$0\sim 100$
- 将获取到的光谱数据作为原数据进行显示
## 加载光谱文件
---
- 读取光谱数据中的波长信息以及波长对应的能量信息
- 进行插值运算
	- 将首位的波长对应的能量设置为$0$(目前查到的资料表示这种操作会更改原有的波长分布，但是应该在数量足够大的情况下误差会很小)
	- 取$\lambda_{i}$与$\lambda_{i+1}$的中间波长$\lambda_{i}+(\lambda_{i+1}-\lambda_{i})*0.5$作为新的对应波长对应原有的$\lambda_{i}$，其中$i\sim N-1$，最后一个波长$\lambda_{N}$对应的能量值不变
	- 获取到新的波长表以及能量表对其进行线性插值计算新的波长数据列表(列表大小为`32768`个)
	- 蒙特卡洛积分的方案为累加积分
- 通过随机$[0\sim 32787]$范围内的索引值来分配每根光线的波长
# Speos可能的方案(待验证)
---
- 获取光谱数据中的波长信息以及波长对应的能量信息
- 插值构造连续$SPD(\lambda)$
	- 一般使用线性插值$$ \begin{array}{l} 
		 \text{对于方程：} \lambda_{i} \le \lambda \le \lambda_{i+1} \\
		 \text{定义：} SPD(\lambda) = P_{i} + \frac{\lambda - \lambda_{i}}{\lambda_{i+1} - \lambda_{i}}(P_{i+1} - P_{i})
		\end{array} $$
- 构建概率密度函数$PDF$
	- 使用梯形积分进行运算$$ \begin{array}{l} 
		 \text{因为：} \int SPD(\lambda)d\lambda \ne 1\\
		 \text{因此对概率密度函数进行积分，计算总能量做归一化，这里使用梯形积分} \\
		 E_{total}= \sum_{i}^{N}\frac{P_{i}+{P_{i+1}}}{2}(\lambda_{i+1}-\lambda_{i}) \\
		 \text{归一化：} PDF(\lambda) = \frac{SPD(\lambda)}{E_{total}} \\
		 \text{此时满足概率为一} \int PDF(\lambda)d \lambda = 1 \\
		\end{array} $$
- 构建$CDF$
	- 定义$CDF(\lambda) = \int^{\lambda}_{\lambda_{min}}PDF(\lambda)d\lambda$，表示不同波长的概率
	- 获取计算好的$PDF(\lambda)$概率表
	- 通过随机函数获取随机值
	- 在表中进行查找随机值落在原始数据的位置
	- 插值计算对应的波长$$ \lambda = \lambda_{i} + \frac{PDF - PDF_{i}}{PDF_{i+1} - PDF_{i}}(\lambda_{i+1} - \lambda_{i})$$
给光线设定波长的方法有两种：
- 直接在生成文件是设置好波长
	- 保证光源文件的不变性，但是在计算的光线数大于文件光源总数时会产生误差
- 在光线出射时设置波长
	- 保证波长分布的一致性，且光线数越多越准确(改动量较大)